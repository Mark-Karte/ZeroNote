// Расширение `.svelte.ts` разрешает рунам работать вне компонента:
// компилятор Svelte обрабатывает такие файлы наравне с `.svelte`.
// Нужно только измерительному стенду при ручном запуске без `--bench-out`,
// когда отчёт показывается прямо в окне.
export const benchReport = $state({ text: '' });
