{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Права основного окна. Держим минимальный набор: каждая новая запись здесь — расширение поверхности, доступной фронтенду.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show"
  ]
}
