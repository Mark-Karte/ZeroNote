//! Текст: кодировки, переносы строк, модель прочитанного документа.
//!
//! Здесь живут инварианты 1 и 5. Ни одна функция этого модуля не трогает
//! файловую систему — только байты и строки. Поэтому всё проверяется
//! обычным `cargo test` без временных файлов и без запуска приложения.

pub mod detect;
pub mod document;
pub mod encoding;
pub mod eol;
