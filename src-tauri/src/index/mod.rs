//! Полнотекстовый индекс проекта: SQLite FTS5.
//!
//! Индекс — это **кэш**, целиком выводимый из файлов на диске. Из этого
//! следует всё остальное: схема не мигрируется, а перестраивается (Р-060);
//! испорченная база не ошибка, а повод построить заново; потеря последних
//! записей при падении означает переиндексацию пары файлов, а не потерю
//! пользовательских данных.
//!
//! Одна база на все корни (Р-059), лежит в папке данных приложения, а не
//! в папке проекта (Р-058): мы не сорим в чужих папках.
//!
//! Вся работа — в фоновом потоке с отменой (инвариант 6). Ввод пользователя
//! не должен ждать индексацию хранилища на десять тысяч файлов.

pub mod graph;
pub mod jobs;
pub mod names;
pub mod query;
pub mod schema;
pub mod writer;

pub use writer::{IndexError, Indexed};
