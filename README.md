# ZeroNote

Редактор для Windows, который совмещает две вещи, сегодня живущие раздельно:
скорость и универсальность Notepad++ — и работу с папками заметок как с
проектами, по модели VS Code.

Хранилище Obsidian открывается как обычная папка. ZeroNote не повторяет чужой
формат: у него свой файл проекта, а связи между заметками (`[[ссылки]]`,
frontmatter, теги) он понимает как свойство markdown, а не как «режим
совместимости». Подробнее — решение Р-022 в [DESIGN.md](DESIGN.md).

> **Состояние: этап 3 завершён, версия 0.3.0.**
> Редактор работает целиком, папка открывается как проект, интерфейс собран
> по дизайн-референсу: семь тем, вшитые шрифты, палитра команд, окно
> параметров, мультикурсор.

Замысел, модель данных, инварианты и журнал решений — в [DESIGN.md](DESIGN.md).

## Что уже работает

### Папка как проект

- Папка открывается корнем (`Ctrl+Shift+O`), корни переживают перезапуск.
- Дерево файлов со слежением за диском: файл, созданный в другой программе,
  появляется сам. Правила игнорирования — в семантике `.gitignore`, включая
  вложенные, плюс свои правила в `zeronote.toml`.
- Быстрое открытие по имени (`Ctrl+P`) с нечётким совпадением: `edtr` находит
  `EditorHost.svelte`.
- Поиск по содержимому проекта (`Ctrl+Shift+F`) с отрывками и переходом
  к совпадению. Индекс — SQLite FTS5, строится в фоне, отменяется, обновляется
  сам по изменениям на диске.
- Подсветка синтаксиса: двадцать языков, включая C, C++, Rust, Python,
  JavaScript, TypeScript и markdown. Код внутри блоков markdown подсвечивается
  своим языком. Цвета — часть темы.
- Блоки кода в markdown выделены подложкой и рамкой, подписаны языком и умеют
  копироваться одной кнопкой. Подпись показывает, чем язык признан, — а если
  не признан, так и говорит.
- Связи между заметками: `[[ссылки]]`, теги, frontmatter, переход
  по `Ctrl`+щелчку и `F12`, панель обратных ссылок. Висячая ссылка видна
  и никуда не ведёт.
- Хранилище Obsidian опознаётся, и настройки исключения переносятся в наш
  `zeronote.toml` по кнопке. Односторонне; в `.obsidian` не пишется ничего.

### Интерфейс

- Семь встроенных тем: Графит, Бумага, Полночь, Нордик, Хвоя, Сепия, Контраст.
  Светлая и тёмная пара следуют настройке Windows. Свои темы — файлом,
  применяются без перезапуска.
- Шрифты `IBM Plex Sans` и `JetBrains Mono` вшиты в приложение: вид не зависит
  от того, что установлено в системе.
- Шапка с логотипом, путём к файлу крошками и полем быстрого открытия.
  Панели с зазорами и скруглением, плотность интерфейса переключается.
- Палитра (`Ctrl+P`): одно поле, три режима по префиксу — файлы, `>` команды,
  `#` теги. Команды показываются с их настоящими сочетаниями.
- Окно параметров (`Ctrl+,`) поверх `settings.toml`: правит файл, сохраняя
  комментарии и порядок ключей.
- Стартовый экран со списком недавних файлов и горячими клавишами.

### Редактор

- Окно с вкладками, перетаскивание вкладок и перетаскивание файлов в окно.
- Создание, открытие, сохранение, «сохранить как», закрытие с подтверждением.
- Кодировки: UTF-8, UTF-16 LE/BE, windows-1251, windows-1252, IBM866, KOI8-R.
  Определяются сами, видны в строке состояния, меняются вручную — отдельно
  «интерпретировать как» и отдельно «преобразовать в».
- Переносы строк CRLF/LF/CR: сохраняются как были; смешанный файл спрашивает,
  к чему привести, и только при первом сохранении.
- Восстановление сессии после перезапуска, включая несохранённые вкладки
  без файла на диске. Черновики пишутся через пару секунд после правки.
- Отслеживание внешних изменений: чистый буфер перечитывается сам, изменённый
  спрашивает, исчезнувший файл предлагает выбор.
- Поиск и замена, в том числе регулярными выражениями.
- Отмена/повтор, переход к строке, операции со строками.
- Строка, столбец и размер выделения в строке состояния; нажатие на них
  открывает переход к строке.
- Мультикурсор: `Ctrl+D` — курсор на следующее совпадение, `Alt`+щелчок —
  курсор в место щелчка, `Escape` — обратно к одному.
- Перенос длинных строк переключается в строке состояния.
- Горячие клавиши в раскладке Notepad++, переназначаются файлом.
- Файлы свыше 50 МБ открываются в режиме только для чтения.

## Чего пока нет

Графа связей между заметками. Создания заметки по висячей ссылке.
Мини-карты и живого превью markdown. Автосохранения по таймеру в файл:
несохранённое хранится в черновиках и переживает падение, но в сам файл
попадает только по Ctrl+S.

Только Windows 10/11, только локальная файловая система. Синхронизация, Linux,
плагины, LSP, сравнение файлов и прочее — вне области первого круга, см. раздел
«Отложено» в `DESIGN.md`.

## Сборка из исходников

Нужны: Rust (msvc), Node 20+, WebView2 (есть в Windows 11 из коробки).

```bash
npm install
```

Отладочный запуск:

```bash
npm run tauri dev
```

Релизная сборка и установщик:

```bash
npm run tauri build
```

Установщик появится в `src-tauri/target/release/bundle/nsis/`.

## Проверки

```bash
npm run check
```

```bash
cd src-tauri && cargo test
```

## Замеры производительности

Цели по скорости — часть спецификации, а не пожелание; регрессия считается
дефектом. Стенд встроен в приложение и включается аргументами командной строки.

```bash
powershell -File bench/perf.ps1
```

Работает только на релизной сборке. Результаты — в `DESIGN.md`, раздел
«Измерения». Отдельные замеры — `-Only startup|open|tree|index|highlight|live`;
последний проверяет инвариант 6: задержку ввода под настоящей индексацией.

```bash
powershell -File bench/crash-test.ps1
```

Проверяет инвариант 4 на живом процессе: правит файлы через интерфейс, убивает
процесс и смотрит, что восстановилось. Скрипты ручных проверок интерфейса —
в `bench/ui/`.

## Для тестировщиков

Сборка 0.3.0: `ZeroNote_0.3.0_x64-setup.exe`, 3,86 МБ.
SHA-256 `127D65CFAB7FCF531EBFCF8A706CD47CAC772E63B8D3E3235067B9035D6C19F7`.

Обновление поверх 0.2.0 ставится как обычно: настройки, темы, открытые вкладки
и несохранённые черновики сохраняются. В `settings.toml` появился раздел
`[editor]` — если файла нет, он создастся с пояснениями; существующий не
трогается, недостающие ключи берут значения по умолчанию.

### Установщик не подписан

Сертификат подписи кода для проекта не покупается, поэтому при запуске
установщика Windows покажет синий экран **«Система Windows защитила ваш
компьютер»**. Это ожидаемо и означает ровно одно: издатель неизвестен Microsoft.

Чтобы продолжить: **«Подробнее»** → **«Выполнить в любом случае»**.

Перед этим имеет смысл сверить контрольную сумму файла с опубликованной вместе
со сборкой:

```powershell
Get-FileHash .\ZeroNote_0.3.0_x64-setup.exe -Algorithm SHA256
```

### Что нужно на машине

Windows 10 или 11 и компонент **WebView2**. В Windows 11 он есть из коробки;
на Windows 10 установщик подтянет его сам, для чего понадобится интернет.
Прав администратора не нужно: приложение ставится только для текущего
пользователя.

### Куда приложение кладёт данные

Приложение не засоряет систему: настройки, темы, сессия и черновики лежат рядом
с исполняемым файлом, в подпапке `data\`.

Установка идёт **только для текущего пользователя**, в `%LOCALAPPDATA%\ZeroNote\`,
и не требует прав администратора. Это не косметика: в `Program Files` папка была
бы недоступна на запись, и черновики несохранённых файлов молча терялись бы.

```
%LOCALAPPDATA%\ZeroNote\
  zeronote.exe
  uninstall.exe
  data\
    settings.toml      настройки, правятся руками
    keymap.toml        переназначение горячих клавиш
    themes\            ваши темы
    session.toml       какие вкладки и папки были открыты
    recent.toml        недавно открытые файлы
    drafts\            несохранённое
    index.db           индекс поиска по проекту — кэш, можно удалять
```

`index.db` — единственный файл, который не жалко: он целиком выводится
из файлов на диске. Удалите его, и он построится заново.

Если папка приложения окажется недоступной на запись — например, при запуске
с флешки с защитой от записи, — приложение перейдёт на
`%LOCALAPPDATA%\ZeroNote-data\` и скажет об этом в строке состояния.

**Деинсталлятор пользовательские данные не трогает** — проверено: после удаления
папка `data\` со всем содержимым остаётся на месте. Чтобы убрать и её, удалите
`%LOCALAPPDATA%\ZeroNote\` вручную.

### Что нового в 0.3.0

Весь интерфейс собран заново по дизайн-референсу. Настройки теперь правятся
и окном параметров, но файл остаётся главным: окно только пишет в него,
не трогая ваши комментарии.

**Одно расхождение с Notepad++, о котором стоит знать:** `Ctrl+D` теперь
добавляет курсор на следующее совпадение, как в VS Code, а дублирование
строки переехало на `Ctrl+Shift+D`. Вернуть по-старому — две строки
в `keymap.toml`.

### На что смотреть в первую очередь

Проект держится на инвариантах — правилах, которые не должны нарушаться никогда.
Если заметите нарушение любого из них, это важнее любой другой находки:

1. **Файл изменился сам.** Открыли, ничего не правили, сохранили — и файл стал
   другим: сменилась кодировка, переносы строк, появилась или пропала пустая
   строка в конце.
2. **Что-то записалось в `.obsidian`.** Туда не должно попадать ничего —
   ни при открытии хранилища, ни при переносе настроек.
   Так же и с самой папкой: открытие её корнем не создаёт в ней файлов.
   `zeronote.toml` появляется только по кнопке и с переспросом.
3. **Файл побился при сохранении.** Особенно если приложение или система
   упали в этот момент.
4. **Пропало несохранённое.** Убейте процесс через диспетчер задач с грязными
   вкладками и запустите снова — всё должно вернуться.
5. **Кодировка определилась неверно** — крякозябры вместо текста.
6. **Ввод стал заедать**, пока в фоне строится индекс большого хранилища.
   Задержка ввода не должна зависеть от того, чем занято приложение.

### Как сообщать о проблемах

К отчёту полезно приложить: версию Windows, версию ZeroNote, что делали, что
ожидали, что получили. Для проблем с открытием файла — его кодировку и тип
переносов строк из строки состояния, а лучше сам файл: угадать кодировку
по описанию невозможно.

Если приложение ведёт себя странно после обновления, посмотрите на полосу
предупреждений вверху окна — туда попадают ошибки в `settings.toml`,
`keymap.toml` и файлах тем.

## Авторство

Mark Karte и Claude (Anthropic). Проект пишется в паре: архитектурные решения
и приёмка — за человеком, значительная часть кода и проектной документации —
за моделью. Журнал решений в [DESIGN.md](DESIGN.md) ведётся так, чтобы каждое
существенное решение можно было проследить и оспорить, кем бы оно ни было
предложено.

## Лицензия

MIT — см. [LICENSE](LICENSE). Правообладатель — Mark Karte.

Вместе с приложением распространяются два шрифта под SIL Open Font License 1.1:
**IBM Plex Sans** (© 2017 IBM Corp.) и **JetBrains Mono**
(© 2020 The JetBrains Mono Project Authors). Файлы и тексты лицензий —
в [src/theme/fonts/](src/theme/fonts/). На остальной код и документацию
это не влияет: OFL распространяется на сами шрифты.
