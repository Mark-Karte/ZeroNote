# ZeroNote — правила проекта

Краткий свод для рабочих сессий. Подробности — в [DESIGN.md](DESIGN.md).
При расхождении между этим файлом и `DESIGN.md` прав `DESIGN.md`.

## Что это

Десктопный редактор для Windows: скорость развёртывания Notepad++ плюс папки
как проекты и удобства VS Code и Obsidian (Р-114). Один разработчик, работа
по вечерам.
Тестировщики — около 35 друзей-разработчиков. Проект будет опубликован в
открытом доступе.

Формат проекта — **свой** (`zeronote.toml`), а не разбор `.obsidian`; из
хранилища Obsidian переносится один раз пара настроек узким переходником.
Связи между заметками (`[[wikilinks]]`, frontmatter, теги) — свойство
markdown-файлов в проекте, а не «режима Obsidian». Решение Р-022.

## Границы первого круга

Только Windows 10/11, только локальная файловая система.

**Вне области — не реализовывать, не закладывать, не «на всякий случай»:**
синхронизация и облако, Linux, Android, шифрованные контейнеры, менеджер паролей,
плагинный API, макросы, пользовательские синтаксисы (UDL), LSP и автодополнение,
сравнение файлов, FTP/SFTP, совместное редактирование.

Возникла идея из этого списка — не реализовывать, а добавить строку в `DESIGN.md`,
раздел «Отложено».

## Инварианты — нарушать нельзя никогда

1. **Чужие файлы неприкосновенны.** Сохраняются исходная кодировка, тип переноса
   строк, наличие/отсутствие финальной новой строки, содержимое frontmatter.
   Никакого автоформатирования и нормализации без явной команды пользователя.
2. **`.obsidian` только для чтения.** Читать можно. Записывать — нельзя ни при
   каких условиях.
3. **Сохранение атомарное.** Временный файл в той же директории, сброс на диск,
   замена целевого. Права и атрибуты исходного файла сохраняются.
4. **Несохранённые данные не теряются.** Черновики пишутся с задержкой ~2 с после
   последнего изменения. После аварийного завершения восстанавливаются все
   вкладки, включая буферы без файла на диске.
5. **Кодировка — часть модели буфера, а не деталь ввода-вывода.**
6. **Ввод не блокирует отрисовку.** Индексация, обход дерева, поиск по папке —
   в фоне и отменяемы.

Если запрос противоречит инвариантам или границам области — сказать прямо и
предложить альтернативу, не выполнять молча.

## Где мы сейчас

**Этап 1 закрыт, метка `v0.1.0`.** Редактор работает целиком: вкладки,
кодировки, переносы, атомарное сохранение, черновики и сессия, внешние
изменения, поиск с регулярными выражениями, раскладка Notepad++, темы.

**Этап 2 закрыт, метка `v0.2.0`.** Корни и `zeronote.toml`, дерево файлов
со слежением, индекс FTS5, быстрое открытие, поиск по проекту, подсветка
синтаксиса, связи между заметками, переходник Obsidian. Приёмка — `DESIGN.md`,
раздел 13. **Инвариант 6 закрыт полностью**: 3,5 мс на ввод под индексацией
против 3,1 мс в покое.

**Этап 10 закрыт, версия 0.10.0.** «Файлы»: вкладка, которая не текст,
ассоциация файлов, картинки, PDF, картинки и таблицы в превью. План —
`DESIGN.md`, раздел 28; приёмка — раздел 29; решения — Р-180…Р-206.

**Выпуск `v0.10.0` выложен**, обновление проверено через сам механизм:
установленная 0.8.0 нашла 0.10.0, показала заметки, поставила
и перезапустилась. Сессия цела — те же вкладки, те же места курсора, та же
тема. Ассоциации прописались при обновлении, а не только при первой
установке. **Версия на машине владельца — 0.10.0.**

**Шаг вышел через версию: 0.8.0 → 0.10.0.** 0.9.0 была выложена, но владелец
на неё не обновлялся. Механизму это безразлично — он смотрит
на `releases/latest`, — но заметки к 0.9.0 не увидел никто.

**Заметки в `latest.json` — простым текстом** (Р-205). Диалог обновления
показывает их как есть, и `**Картинки.**` вышло на экран со звёздочками.
Найдено на живой проверке; заметки переписаны и выложены заново до установки.
На странице выпуска остаётся markdown — там он и разбирается.

Показатели: тёплый старт 401 мс, ввод под индексацией 3,9 мс. Установщик
**4,92 → 5,38 МиБ**, плюс 471 КиБ — почти весь прирост это pdf.js, и **план
обещал втрое больше**: несжатым он весит 1,62 МиБ, но NSIS ужимает JavaScript
больше чем втрое. Новых зависимостей ядра этап не принёс ни одной.

**Условия замера старта закреплены в стенде** — долг этапа 9 закрыт.
`perf.ps1` отказывается мерить при живой копии той же сборки и на время
замера убирает сессию, возвращая её в `finally`. **Числа старта до и после
этого уточнения впрямую несравнимы.** Разброс между прогонами (383, 411,
401 мс подряд) снова оказался больше разницы между версиями.

**Приёмка нашла три вещи, и две из них починила.**

- **Стенд завис на новом режиме**: список режимов был в двух местах, второй
  забыли дописать. Теперь на его месте условие. Своя защита стенда
  сработала — он сказал «завершилось, не записав отчёт», а не промолчал.
- **pdf.js лежал в пути запуска**: обычный импорт клал его в главный кусок
  сборки, 566 КиБ вместо 142. Показ грузится по требованию. **На тёплый
  старт это не повлияло** — выигрыш утонул в разбросе, и честно сказано:
  измерить его не удалось.
- **Цена таблиц на движении курсора** — 0,42 мс против 0,03 без них. Мера:
  отпечаток выделения по номерам строк, совпал — пересчитывать нечего.
  Стало 0,08 мс.

**Плата за превью появилась там, где обещали.** На этапе 9 её не было
(1,0 против 1,0 мс): обход шёл по видимым строкам. Таблицы обходят всё
дерево (Р-203) — правка стала дороже на полмиллисекунды: 1,5 против 0,9.
Ввод в C++ того же размера стоит 3,3–3,8 мс, то есть markdown с превью
по-прежнему вдвое дешевле кода.

**Показ картинки и PDF измерен** (`perf.ps1 -Only media`): картинка 1/5/15
МиБ — 12+5, 59+22, 150+66 мс (доставка и разбор движком); доставка PDF
1/8/32 МиБ — 8, 57, 195 мс. Разбор pdf.js не меряется: осмысленное число
даёт только настоящий документ, а он невоспроизводим на чужой машине.

Главное, что этап изменил в проекте:

- **Вид вкладки живёт в ядре** (Р-180) — поле рядом с путём: `text`,
  `settings`, `image`, `pdf`. Каждый вид пришёл вместе со своим кодом
  (Р-186). Отсюда: вид в сессии (запись без вида — текст), команды перестали
  быть безусловными, строка состояния зависит от вида.
- **Текстовой части у не-текста нет, и это видно типу** (Р-188):
  `Tab.editor: TabEditor | null`. Компилятор находит каждое место, где нужен
  текст. Так нашлась настоящая ошибка: вкладка параметров попадала бы
  в черновики как безымянный буфер.
- **Байты живут ровно столько, сколько вкладка на экране** (Р-193) — ни
  ядро, ни фронтенд их не хранят. Предел на картинку 16 МиБ, на PDF 64 МиБ:
  у первой строка `data:` на треть длиннее файла, у второго двоичный ответ
  (Р-196).
- **Сетевые адреса не загружаются никогда** (Р-202) — не из-за CSP, а из-за
  Р-118: соединение открывается только по нажатию, а открытая чужая заметка
  не нажатие.
- **Политика безопасности окна не изменилась ни на строку** (Р-200), и это
  была главная проверка этапа. pdf.js шестой версии не просит `unsafe-eval`,
  рабочий поток грузится из `'self'`, байты идут по IPC. Проверено
  и в отладочной, и в выпускной сборке.
- **Одно приложение — один процесс на папку данных** (Р-191). Ключ — папка
  данных, а не идентификатор: отладочная сборка и установленная обязаны
  уживаться. Второй экземпляр не создаёт окна и живёт 68 мс.
- **Ассоциации: «Открыть с помощью», а не умолчание** (Р-190). Умолчание
  с Windows 10 защищено, и назначает его человек — в параметрах системы,
  куда ведёт кнопка в наших параметрах (Р-195).
- **Блочное превью живёт в поле состояния** (Р-203): замена через границу
  строк меняет высоту документа, и от плагина CodeMirror её не принимает —
  молча. **Тесты это не ловят**, как и в случае Р-169.
- **Неприменимая команда гаснет в меню, но не исчезает** (Р-189); `Ctrl+G`
  контекстный — строка в тексте, страница в PDF (Р-198); поиска по PDF нет,
  и `Ctrl+F` отвечает словами (Р-197).

**Что осталось незакрытым:** `![[рисунок.png]]` (разрешается по имени через
индекс, а индекс текстовый) — в «Отложено»; поиск по PDF — решение
владельца; быстрое открытие не находит картинки и PDF, потому что палитра
берёт список из индекса.

**Этап 11 закрыт, версия 0.11.0.** «Области редактора»: дерево областей
в ядре, зеркала одного файла, перетаскивание между областями и на край,
сочетания VS Code. План — `DESIGN.md`, раздел 30; приёмка — раздел 31;
решения — Р-207…Р-213. Ответы владельца от 2026-09-07: один файл
в нескольких областях — да, зеркалами (Р-209); сочетания как в VS Code;
разделять все виды вкладок; мелочи «номера строк только в коде»
и «панель разметки над колонкой» и долг быстрого открытия — этап 12.

**Этап 12 идёт** — «Мелочи владельца и долги», `DESIGN.md`, раздел 32.
Шесть задач (80–85): номера строк только в коде (Р-213), панель разметки
над колонкой, картинки и PDF в быстром открытии, `![[рисунок.png]]` через
список имён всех файлов в индексе, долги областей, приёмка 0.12.0.
**Задачи 80–85 сделаны** (2026-09-08); следующая — 86.
**В этап добавлены задачи 85 и 86** — «назад и вперёд по местам курсора»
и «вернуть закрытую вкладку»; приёмка стала 87. Обе предложены мной
в ответ на вопрос владельца про большую идею и приняты им. Способ вызова
решён заранее: у навигации есть кнопки в шапке, у возврата вкладки —
только сочетание и меню. **Все пять
вопросов закрыты ответами владельца** (`DESIGN.md`, раздел 32, «Решения
владельца»): панель разметки — настройкой; имена всех файлов в индексе —
да; из долгов областей делать только курсор зеркала в сессии; **поиска
по PDF не будет** — ушёл в «Отложено» как закрытый вопрос, а не как
отложенный; своей большой идеи у владельца нет, предлагать её теперь мне.

**Задача 85 сделана**: назад и вперёд по местам курсора. `Alt+←`, `Alt+→`
и две стрелки в шапке слева от крошек.

- **Запись делают два события в двух точках**: смена активной вкладки —
  эффектом в `AppShell`, дальний прыжок курсора — слушателем обновлений
  редактора. Порог прыжка десять строк, как в VS Code; правка прыжком
  не считается.
- **История не знает про вкладки** (`state/history.svelte.ts`): два стека
  и два крючка, `cursorOf` и `goTo`. Тест гоняет её на выдуманном мире.
- **Живая проверка нашла две ошибки, обе мимо тестов.** Переключение
  вкладки считалось прыжком курсора: состояние подменяется в том же
  представлении, и строки считались в двух разных документах — лечение
  «документ обязан быть тем же». Переход менял область и вкладку двумя
  вызовами, и промежуточное состояние записывалось как место — лечение
  «один вызов `setActiveTab`, он сам делает область активной».
- **Состояние истории не видно ниоткуда**, поэтому проверялось временным
  счётчиком записей в шапке. Приём годится и дальше.

**Задача 84 сделана**: курсор зеркала в сессии. Долг этапа 11 закрыт,
два других долга областей оставлены по решению владельца.

- **Курсоры живут в снимке области, а не буфера**: в снимке буфера курсор
  один, а представлений у файла столько, сколько областей (Р-209).
- **Место выбрано ради отката**: у снимка раскладки нарочно нет
  `deny_unknown_fields`, у снимка буфера он есть. Пока область одна,
  раскладка не пишется, и снимок выглядит как у 0.10.0.
- **Ядро курсоров не хранит** — принимает плоский список от фронтенда,
  раскладывает по областям, при восстановлении отдаёт обратно.
- **Курсор ждёт своё представление** в `TabEditor.restored`: зеркало
  создаётся лениво, а сессия восстанавливается вся сразу. Главное тоже
  берёт курсор по области — какая станет главной, заранее не известно.

**Задача 83 сделана**: `![[рисунок.png]]`. Запись вышла из «Отложено»,
где лежала с этапа 10.

- **Три ключа вместо двух** (Р-217): к `rel_key` и `name_key` добавился
  `file_key` — имя целиком. На заметку ссылаются без расширения, на вложение
  с ним, и это разные вопросы к индексу. Схема индекса — версия 4.
- **Порядок вопросов и есть правило**: путь, имя целиком, имя без
  расширения среди прочитанных, имя без расширения среди всех. Отсюда
  «заметка важнее вложения» и «вложение без тёзки всё равно находится».
- **Показ — своя команда** `preview_embed`: цель здесь имя, а превратить
  имя в путь умеет только индекс. Байты дальше идут тем же путём (Р-201).
- **Вставка не-картинки остаётся исходником целиком.** Найдено глазами:
  первая версия прятала скобки и оставляла на экране `!заметка`.
- **Подсказка смотрит на саму запись** (Р-218): после `![[` предлагает
  картинки, после `[[` — только заметки.

**Задача 82 сделана**: картинки и PDF в быстром открытии. Причина, по
которой их не находило, лежала в индексе, а не в палитре.

- **Строка в индексе есть у каждого файла проекта** (Р-216), содержимое
  читается только у текстовых и только до предела из настроек. Предел
  размера стал пределом на содержимое, а не на присутствие в индексе.
- **Признак `has_text` в строке** — для тех, кто спрашивает не «какие
  файлы есть», а «на что можно сослаться». Поиск по проекту отсеивает
  такие файлы сам, соединяясь с `content`.
- **Ссылки и подсказка `[[` намеренно оставлены как были**: правила
  приоритета «заметка или вложение» — задача 83. Граница закреплена
  тестом `links_do_not_reach_images_yet`; подсказка берёт отдельный
  запрос `text_files`.
- **Двоичный файл больше не читается целиком** — распознаётся по первым
  восьми килобайтам. До задачи 82 картинка читалась в память полностью,
  чтобы тут же быть отброшенной.
- **Схема индекса — версия 3**, база перестраивается (Р-060). Во фронтенде
  не изменилось ничего: значок берётся по имени файла, открытие идёт через
  то же ядро, что с этапа 10 само решает вид вкладки.
- **Тест, который упал не зря**: обход дошёл до собственной базы индекса,
  лежавшей в индексируемой папке (23 файла вместо 20). В приложении она
  лежит в папке данных (Р-058); тест переучен класть её в стороне.

**Задача 81 сделана**: панель разметки над колонкой. Настройка
`[editor] markdown_bar_width` — `column` или `full`, по умолчанию `column`.

- **Едут кнопки, а не полоса.** Полоса с чертой остаётся во всю ширину
  области, к колонке съезжает внутренний слой с кнопками: черта отделяет
  панель от текста, это граница области, а не колонки.
- **Колонка сильнее настройки** (Р-215): нет колонки — панель во всю
  ширину при любом значении. Само значение при этом не сбрасывается.
- **Шрифт редактора на слое кнопок стоит ради единицы `ch`**, а не ради
  вида: колонка задана в знаках (82ch), и знак интерфейсного шрифта дал бы
  другую ширину. Кнопкам шрифт не достаётся — у них он задан токеном,
  а не `inherit`.

**Задача 80 сделана**: номера строк только в коде. Настройка
`[editor] line_numbers` — `always`, `never`, `code`, по умолчанию `code`.

- **Прячется число, а поле остаётся** (Р-213). `lineNumbers()` из набора
  расширений не исчезает — меняется только подпись, она становится пустой.
  Закладки помечают ячейку этого поля, и убрав поле, пришлось бы решать
  заново, где им жить. Поле сужается само: его ширину задавал номер.
- **Закладке понадобился свой знак** — её показывало число, покрашенное
  акцентом. Знаком `content: '•'`, а не фигурой из размеров: он едет
  за кеглем и слушается `color`, как стрелка табуляции. **Тестом это
  не ловится**, найдено снимком живого окна.
- **Класс приезжает от состояния**, а не от компонента
  (`EditorView.editorAttributes` в том же отсеке): представление в области
  одно, а состояний в нём столько, сколько вкладок.
- **Прозой считается только markdown** (Р-214). Файл без языка — `.txt`,
  безымянный буфер, незнакомое расширение — номера сохраняет: чаще всего
  это лог или вставленный кусок кода. Смотрим на язык вкладки, а не на имя
  файла, поэтому выбор языка руками работает в обе стороны.
- **Отсек добавлен в список копируемых в зеркало** — иначе у зеркала
  вкладки с выбранным вручную языком номера решались бы по имени файла.

**Выпуск `v0.11.0` выложен** 2026-09-08, обновление проверено через сам
механизм: установленная 0.10.0 нашла 0.11.0, показала заметки, поставила
и перезапустилась; сессия владельца цела, разделение проверено на его
`README.md`. **Версия на машине владельца — 0.11.0.** Состояние выпуска
проверять `gh release list`.

Показатели: тёплый старт 377 мс, ввод под индексацией 4,1 мс, ввод
в покое 3,8. **Второе представление, в которое не печатают, не стоит
ничего** (3,4 мс рядом с областью markdown). **Зеркало стоит вдвое —
6,9 мс**: правка едет в главное и обратно, перерисовываются два
представления; это самый дорогой путь ввода, и стенд его сторожит.
Тёплый старт с разделённой сессией — 434 против 419 мс с той же парой
файлов в одной области. Установщик 5,38 → 5,43 МиБ, +47 КиБ, новых
зависимостей нет. `crash-test` теперь с раскладкой: после kill вернулись
обе вкладки и обе области.

**Приёмка нашла: разделение пересоздаёт область, которую делят.** Дерево
рисуется так, как хранится, и на месте узла «область» появляется узел
«разделение» — компонент прежней области уничтожается вместе с её
`EditorView`, содержимое подставляется заново из запаса. Нашёл стенд: он
держал прежнюю ссылку и «зеркало не монтировалось». Записано долгом;
чинить — только если станет заметно глазу, на мегабайте не заметно.

**Долги этапа**: пересоздание области при разделении; `edit.undo-cursor`
в зеркале молчит; зеркала восстанавливаются с курсором главного; масштаб
картинки и страница PDF общие для зеркал; подсветка зоны сброса
не проверена глазами.

**Задача 78 сделана**: команды и панели. `Ctrl+1…9` — область по номеру,
`Ctrl+Alt+←/→` — перенос вкладки в соседнюю область (за последней — новая
с краю), `view.close-pane` без сочетания; проверено на живом окне.

- **Два места смотрели в главное состояние вместо видимого**: строка
  состояния и оглавление показывали бы курсор соседней области. Заведена
  `visibleState(tab)` в `state/tabs`. Остальные потребители «текущего»
  идут через `editorView()` или переданное представление — они в порядке.
- **Панель поиска переезжает с фокусом**, подсветки с прежнего
  представления снимаются пустым запросом (`applied` в `state/search`).
- **«Закрыть другие» — в своей области**, «закрыть все» — на окно.
- **Стенд**: сочетание без `-At` после смены переднего плана теряется;
  надёжно только «щелчок в область, потом клавиши».

**Задача 77 сделана**: вкладки между областями. Перетаскивание в чужую
полосу, в середину и на край области (новое разделение), «Открыть сбоку»
с вкладки и из дерева; проверено на живом окне по файлу сессии.

- **Зоны как в VS Code** (`ui/pane-drop.ts`): четверть у края — разделение,
  середина — перенос, полоса — место между вкладками. Подсвечивает
  область-приёмник через общее состояние `state/tab-drag`.
- **Сброс на край — одна операция ядра** `move_to_split`; разделение
  получило сторону (`split_at`, `first`).
- **Захват указателя держит полоса, а не вкладка**: перестановка переносит
  узел в DOM, и Chromium снимает захват — раньше это молча ограничивало
  перетаскивание одним шагом. Нашлось трассой событий.
- **«Открыть сбоку» из дерева — сначала область, потом файл**; пустая
  область после неудачи закрывается.

**Задача 76 сделана**: второе представление. `Ctrl+\` делит область
вправо, новая получает зеркало текущей вкладки; проверено на живом окне
вместе с зеркалами и восстановлением разделённой сессии.

- **Дерево рисуется так же, как хранится**: `ui/PaneTree.svelte` зовёт
  сам себя, область — `PaneView`. Полоса вкладок ушла с уровня окна
  внутрь области; панель поиска одна и стоит над активной областью.
- **Фокус назначает щелчок на стадии захвата** (Р-210): панель разметки
  гасит `mousedown`, и без захвата команда шла бы в прежнюю область.
  Акцентная черта — только у активной области.
- **Зеркала по Р-209** (`editor/mirror.ts`, `slotFor`/`applyToPrimary`
  в `state/tabs`): главное получает первая область, зеркало создаётся
  из него с копией отсеков через `Compartment.get`; рассылка всегда через
  главное с пометкой-аннотацией источника; отмена — `runHistory` над
  главным, у зеркала истории нет; все переконфигурации — одна
  `reconfigure`; смена раскладки сверяет зеркала и передаёт главное.
- **Закрытие зеркала не спрашивает про несохранённое**
  (`closeTab(id, pane)`): буфер живёт в других областях.
- **Предел размера — токен `control-pane-min-size`, меряет разметка**
  (`ui/pane-size.ts`, Р-212).
- **Круг импортов `panes → persist → tabs → panes` нашёлся тестом**:
  `panes` зовёт `persist` импортом по требованию.
- **Долги**: `edit.undo-cursor` в зеркале молчит; зеркала восстанавливаются
  с курсором главного; масштаб картинки и страница PDF общие для зеркал.

**Задача 75 сделана**: модель областей. На экране ничего не изменилось,
и это проверено на живом окне.

- **Дерево областей живёт в ядре** (`model/layout.rs`, Р-207), сессия
  пишет его только когда областей больше одной: файл с одной областью
  читается 0.10.0 при откате. Тот же приём, что у `kind` вкладки.
- **Порядок в реестре буферов больше ничего не значит.** Порядок вкладок —
  свойство области; `reorder_buffer` удалён. Команды открытия кладут буфер
  в активную область одним `show`.
- **Закрытий два**: `close_buffer` — совсем и отовсюду, `remove_tab` —
  из одной области при живом зеркале. Оба возвращают раскладку целиком.
- **Во фронтенде две тривиальные копии логики** — «добавить в активную
  область» и «сделать вкладку активной». Остальное через ядро, ответ
  применяется целиком. `tabs.activeId` исчез: активная вкладка —
  `activeTabId()` из `state/panes`.
- **`editorView()` и `pdfView()` отвечают за активную область**; ни одна
  команда не хранит представление дольше одного вызова (Р-208).
- **В стенде появился `drag.ps1`**: перетаскивание иначе не проверить.

**Этап 9 закрыт, версия 0.9.0.** «Превью»: непрерывная сборка, отрывок
поиска, вкладка «Темы», живое превью markdown целиком. План — `DESIGN.md`,
раздел 26; приёмка — раздел 27; решения — Р-158…Р-179.

**Выпуск `v0.9.0` выложен** 2026-09-06 — уже после того, как эта запись
говорила обратное, и поправлена она была только на этапе 10. Владелец на него
не обновлялся и шагнул сразу на 0.10.0. **Состояние выпуска живёт на GitHub,
а не здесь: проверять `gh release list`, а не память файла.**

Показатели: тёплый старт 421 мс, ввод под индексацией 3,9 мс. **Плата
за живое превью не нашлась**: правка в markdown с превью и без — 1,0 против
1,0 мс, движение курсора 0,04 против 0,03 мс, то есть меньше разброса.
Причина в устройстве: обход только по видимым отрезкам, полсотни строк
независимо от размера файла.

**Приёмка нашла своё, и находка про сам замер.** Первый прогон показал
тёплый старт 277 мс вместо 409 — «улучшение», которого этап не делал.
Виноват был **живой сосед**: рядом было открыто окно той же сборки, и оно
держало среду WebView2 тёплой. Одна и та же сборка: **422 мс без соседа
и 292 мс с соседом**; пересобранная из метки 0.8.0 дала в тот же час 423 мс.
Правило записано в «Особенности окружения».

**Долг Р-169 закрыт.** У соседей (`quotes`, `code-blocks`) устройство то же,
но они создаются **вместе с языком**, а `LanguageState.init` разбирает
синхронно первые 3000 знаков — оттого дерево у них уже есть. Дыра всё равно
была: первый экран длиннее трёх тысяч знаков дорастает фоновой работой.
Обоим добавлено сравнение деревьев.

**Установщик 4,90 → 4,92 МиБ**, плюс 25,3 КиБ. Новых зависимостей нет.

**Два дефекта пришли от владельца, работающего в программе каждый день** —
новый источник задач, лучший из возможных.

- **Строка под курсором показывает исходник** (Р-158) — правило, на котором
  держится всё превью. Из него следует, что править разметку можно всегда
  и что вопрос «как курсор ходит сквозь спрятанное» почти исчезает. Плата:
  украшения пересобираются на смену выделения, то есть на пути ввода;
  приёмка меряет ввод в markdown с превью и без.
- **Превью включено по умолчанию** (Р-159, решение владельца). Цена названа:
  человек, открывший чужой `.md`, увидит не файл, а отображение. Взамен —
  тестировщики его найдут. Только markdown.
- **Копируется файл, а не экран** (Р-160). Спрятанный текст остаётся
  в документе, поэтому копирование отдаёт исходник со знаками разметки.
  Правило проверки задачи 57 этим отменяется и заменяется новым.
- **Таблицы и картинки в превью — этап 10**: у картинки в заметке и у вкладки
  с картинкой один путь загрузки байтов, делать его дважды незачем.
- **Этап 10 — «Файлы»**: PDF через pdf.js (~1,5 МиБ), картинки, вкладка,
  которая не текст.

**Задача 66 сделана**: callout-ы. `> [!tip] Совет` — карточкой с чертой,
значком и заголовком. Пять ролей: заметка, подсказка, внимание, опасность,
цитата; двадцать шесть типов Obsidian сведены к ним.

- **Своего узла разбора нет, вопреки плану** (Р-177). Callout — не новый
  синтаксис, а цитата с первой строкой в известном виде; разбор цитат уже
  есть и считает вложенность сам, свой блочный пришлось бы поставить вместо
  него целиком. `==выделение==` отличалось тем, что разбора не было вовсе.
  Знак разбирает чистая `parseCallout` (`editor/callouts.ts`).
- **Незнакомый тип становится заметкой**, а не остаётся `[!придумал]`
  посреди текста.
- **Карточка остаётся под курсором, знак — нет** (Р-178). Р-158 применяется
  только к знаку: карточка — оформление строки, как черта у цитаты, и её
  исчезновение заставляло бы блок прыгать при каждом заходе курсором.
- **Своих слов на экране не появляется** (Р-178). У callout-а без заголовка
  Obsidian пишет «Note»; мы показываем только значок. Превью прячет и рисует,
  но не сочиняет.
- **Цвет роли не заливает фон** (Р-179). Заливка напрашивалась и была
  в первой попытке — **тест токенов не пропустил `color-mix`**, и это верно
  по существу: цветная подложка под текстом это четвёртый фон, а проверка
  читаемости (Р-143) считает контраст на трёх. Подложка — `bg-canvas`,
  роль утопленного, как у блоков кода; цвет роли назван чертой, значком
  и заголовком.

**Задача 65 сделана**: превью блочной разметки. Решётка заголовка, скобка
цитаты, скобки и адрес ссылки, обе пары скобок вики-ссылки; `---` рисуется
чертой.

- **Знак прячется вместе с пробелом за ним** (Р-173), иначе текст съезжает
  вправо на знак относительно соседних строк. Из того же — знак списка
  не прячется: дефис держит отступ, списки в превью не входят.
- **Родитель проверяется не везде, и разбирать надо каждый случай** (Р-174).
  У **цитаты** — не проверяется: у второй строки `QuoteMark` лежит внутри
  `Paragraph` (ленивое продолжение CommonMark), а рождает его только разбор
  цитаты. У **заголовка** — обязательно: `HeaderMark` бывает и решёткой,
  и подчёркиванием `===`, а подчёркнутый заголовок не прячем (осталась бы
  пустая строка). У **ссылки** — отделяет ссылку от картинки: узлы те же,
  прятать у картинки нечего (этап 10).
- **Вики-ссылку разбирает наш поиск, а не дерево** (Р-175): lezer видит
  внутри `[[Заметка]]` обычную ссылку и помечает **внутренние** скобки —
  по дереву на экране осталось бы `[Заметка]`. Выражение вынесено
  в `wikilinkSpans` и одно на подсветку и превью. `[[цель|подпись]]`
  показывается подписью, `[[цель#раздел]]` — целиком.
- **Черта отменяет правило задачи 57** (Р-176), и это было предсказано
  в Р-160: Р-152 описывает исходный режим, курсор на строке возвращает дефисы.

**Задача 64 сделана**: живое превью markdown — знаки вокруг текста.
Настройка `[editor] live_preview`, по умолчанию включена (Р-159), только
markdown. Переключатель «превью / исходник» — в строке состояния.

- **Прячется знак внутри известного родителя, а не «узел разметки»**
  (Р-168). `CodeMark` — это и обратная кавычка строчного кода, и ограждение
  блока из трёх кавычек; спрятать второе значило бы стереть границу блока,
  где разметка и есть содержимое. Заголовки, цитаты и списки не входят
  по существу: там знак не окружает текст, а относится к строке (задача 65).
- **Украшения пересобираются и когда приезжает разбор** (Р-169) —
  `syntaxTree(update.startState) !== syntaxTree(update.state)`. **Найдено
  на живом окне:** отсек превью кладётся при создании состояния, а язык
  приезжает позже своим отсеком; дерева ещё нет, украшения пустые, и файл
  открывался со знаками, раскрываясь от первого щелчка. **Тестом это
  не ловится** — проверка зовёт сборку на готовом дереве. У соседей
  (`quotes`, `code-blocks`) устройство то же, и работают они по случайности
  порядка событий: проверить в приёмке.
- **Строка под курсором показывается исходником** (Р-158) — проверено
  на живом окне: курсор на строке, и она одна со звёздочками, соседние без.
- **Копируется и сохраняется исходник** (Р-160): набрал `**новое**`,
  сохранил, в файле ровно `**новое**`. Инвариант 1 не задет: превью — слой
  рисования.
- **Переключатель показывает вкладку, а меняет настройку** (Р-170), как
  перенос. Только над markdown. Заодно смена языка вручную теперь
  пересчитывает и превью, и перенос — давняя недоделка задачи 58.

**Задача 63 сделана**: вкладка «Темы» — третья в параметрах. Список карточек
с образцом, «создать свою на основе этой», «открыть папку тем».

- **Образец рисуется настоящими цветами темы** (Р-165), потому что проверку
  читаемости мы прогоняем только на встроенных, и у чужой темы единственный
  судья — глаз. Цвета считает ядро (`theme::samples`): подстановка палитры
  и донашивание недостающего живут там, вторая реализация разошлась бы.
- **Восемь цветов, а не вся таблица**, и **отдельной командой**, а не полем
  состояния оформления: состояние собирается на каждом запуске, а вкладку
  открывают раз в месяц. Но образцы перезапрашиваются на каждое изменение
  оформления — правка цвета в файле видна на карточке сама.
- **Копия сохраняет комментарии** (Р-166): `toml_edit`, меняются ровно две
  строки — `id` и `name`. У встроенных тем в заголовке разбор палитры
  и источник, а копию делают затем, чтобы править руками.
- **Идентификатор копии обязан смениться**: пользовательская тема с id
  встроенной перекрывает её, и копия с тем же id съела бы оригинал молча.
  Он же становится именем файла, поэтому чистится от лишних знаков;
  запись идёт с `create_new` — поверх не пишем никогда.
- **Щелчок при «Как в Windows» назначает пару** своего оформления (Р-167),
  а не отменяет режим: отменить настройку, включённую осознанно, в ответ
  на действие, которое об этом не предупреждало, нельзя.
- **Выбор темы ушёл со вкладки «Настройки» целиком.** Два места для одной
  настройки не разошлись бы в поведении, но разошлись бы в глазах.
- **Тест токенов поймал два выдуманных мной токена** — `--zn-opacity-disabled`
  и `--zn-radius-full`. Недоступность у нас выражается цветом, а не
  прозрачностью; круглого радиуса в наборе нет.

**Задача 62 сделана**: отрывок поиска показывает совпадение. Ведущий
контекст обрезается до десяти знаков, дальше — многоточие (`ui/snippet.ts`).

- **Правится показ, а не поиск** (Р-164). Отрывок из FTS5 исправен —
  совпадение в строке есть всегда; не помещалось оно в 240 пикселей.
  Уменьшать число слов в `snippet(…, 12)` нельзя: это режет и то, что
  **после** совпадения, а читают как раз вперёд.
- **Знаками, а не словами**: отрывок рисуется шрифтом редактора, он
  моноширинный. 240 px минус поля — около 32 знаков при кегле 11; на самой
  узкой панели (160 px) — около двадцати. Десять знаков подводки оставляют
  совпадение видимым и там.
- **Ширина панели не измеряется**: её тянут мышью, и отрывок, меняющий
  обрезку при перетаскивании края, читать невозможно.
- **Проверено на живом окне парой «до и после»**: было
  «…окна и перенесётся по её право…» без подсветки вовсе, стало
  «…уедет за **экран**.». Цена видна там же: когда совпадение заканчивает
  отрывок, строка выходит вдвое короче панели. Отдать место под подводку —
  вернуть ту же беду на узкой панели; предсказуемость дороже плотности.
- **Строка осталась одной.** Две показали бы больше контекста, но вдвое
  уменьшили бы число видимых результатов, а жалоба была «не видно
  совпадения», а не «мало контекста».

**Задача 61 сделана**: непрерывная сборка. `.github/workflows/ci.yml` —
те же три проверки, что стоят ниже перед коммитом, на каждое изменение
в `main` и на каждый запрос слияния. Значок состояния — вверху README.

- **Только Windows** (Р-161), и это не экономия минут: `fsx/recycle.rs`
  и `clipboard.rs` целиком под `cfg(windows)`, на Linux ядро не соберётся.
  Матрица платформ у проекта с одной платформой — время, оплаченное ничем.
- **Проверок ровно три**, `clippy` и `-D warnings` сознательно не добавлены:
  правило, ловящее не дефект, а несовпадение вкуса, съедает вечер так же,
  как настоящая ошибка, а новые предупреждения приносит новый компилятор сам.
  Что в CLAUDE.md, то и в сборке.
- **Сборка фронтенда идёт перед `cargo test`**: `lib.rs` зовёт
  `tauri::generate_context!`, а тот вшивает `dist/` — без него ядро
  не компилируется вовсе.
- **Действия только GitHub** (Р-162): `checkout`, `setup-node`, `cache`.
  Обёртки вроде `Swatinem/rust-cache` удобнее, но это чужой код, который
  исполняется в нашем репозитории и от нашего имени. Rust на образе уже стоит.
- **Ключа подписи в секретах нет и не будет** (Р-129 + Р-162). Сборка
  не делает установщика и не выпускает версий: выпуск — ручное действие
  на машине владельца, потому что решение выложить версию принимает человек.
- **`CARGO_INCREMENTAL: 0`** — инкрементальная сборка в CI бесполезна,
  а место занимает вдвое; ровно она раздула локальный `target/debug` до 44 ГиБ.
- **Первый прогон провалился и окупил задачу сразу** (Р-163). Упал не новый
  код, а `imported_rules_actually_hide_files` с этапа 2: `temp_dir()` на
  машине сборки — короткое имя `C:\Users\RUNNER~1\...`, корень приложение
  разворачивает, и тест спрашивал про ту же папку другой записью. **Правило:
  тест, сравнивающий пути, берёт их в том виде, в каком их хранит приложение**
  (`normalize`). Приложение не менялось: разворачивать путь внутри
  `is_ignored` — это системный вызов на каждый файл при обходе дерева.
- **Второй прогон нашёл то же ещё в двух тестах** (`tests/project.rs`):
  `cargo test` **останавливается на первой упавшей цели**, и первый прогон
  показал только `obsidian`. Правильный ход — воспроизвести условие машины
  сборки локально (`TMP`/`TEMP` → короткое имя) и прогнать с `--no-fail-fast`,
  а не платить прогоном в пять минут за каждую находку.
- **Холодный прогон — 4 м 45 с**, из них `cargo test` 3 м 54 с. Кэш
  сохраняется только на успешном прогоне.

**Замечания владельца из работы** (2026-09-06, подробности и диагноз —
`DESIGN.md`, раздел 26, «Найдено владельцем в работе»):

- **Параметры — вкладкой, а не режимом окна**, и содержимое по центру.
  Вкладка параметров — **то же расширение модели вкладок, что PDF в этапе 10**;
  делать порознь вдвое дороже.
- **Ассоциация файлов и «Открыть в ZeroNote» по правой кнопке** — работа
  установщика: `HKCU\Software\Classes`, глагол для файла и для папки.
  Приём путей из командной строки уже есть.
- **Дефект: папка, брошенная в окно, даёт «Отказано в доступе».**
  **Исправлено (Р-171):** папка открывается корнем, пути раскладывает ядро
  одним вызовом `split_paths`. Заодно добавление корня переключает полосу
  на дерево — иначе корень оказывался на невидимой панели.
- **Дефект: кнопки панели разметки обрезаются на узком окне.**
  **Исправлено (Р-172): перенос на вторую строку.** Прокрутка, сделанная
  первой, оказалась половинчатой — **обычное колесо горизонтальную полосу
  не двигает**, нужен горизонтальный ролик или свой обработчик. Разница
  с полосой вкладок по существу: вкладок сколько угодно, а кнопок
  четырнадцать, и перенос кончается на второй строке.

**Идеи владельца на будущее** (записаны 2026-09-06, ни одна не назначена
на этап; подробности и оценки — `DESIGN.md`, раздел 26):

- **Области редактора** — вкладки в нескольких областях, как в VS Code.
  **Это этап целиком, а не задача:** сегодня у приложения одно представление
  (`editor/current.ts`), и каждая команда спрашивает у него «текущее».
- **Номера строк не нужны markdown.** Настройка — полдня, но **в поле
  номеров живут закладки** (задача 37): убрав номера, надо решить, где
  показывать метку. Вопрос той же природы, что решался в задаче 58.
- **Выравнивание панели разметки** настройкой. Дёшево; стоит связать
  с читаемой шириной — панель по центру встаёт над колонкой, и это,
  похоже, и есть правильное умолчание.
- **Снимок Obsidian от владельца — образец для задач 64–66**: callout-ы
  карточками, таблицы сеткой, цветные плашки. Там же видно Р-158 в чужой
  реализации: `==начать==` со знаками, потому что курсор на этой строке.

**Этап 8 закрыт, версия 0.8.0.** «Оформление»: шесть тем, значки в меню
и палитре, плотность и границы, оформление markdown в исходнике, читаемая
ширина, панель закладок. План — `DESIGN.md`, раздел 24; приёмка — раздел 25;
решения — Р-143…Р-157.

**Выпуск `v0.8.0` выложен**, обновление проверено через сам механизм:
установленная 0.7.0 нашла 0.8.0, показала заметки из манифеста, поставила
и перезапустилась. Сессия владельца цела — те же вкладки, тот же курсор
(строка 8, столбец 17), та же тема «Контраст». **Версия на машине
владельца — 0.8.0.**

**Первая сборка шла без подписи**, и `.sig` не появился — ровно то, о чём
предупреждает второй шаг порядка выпуска. Замеры сняты на ней, выложена
пересобранная с ключом; отсюда 335 байт разницы в размере установщика.

Показатели: тёплый старт 409 мс (было 406), ввод под индексацией 3,8 мс
(было 3,9). **Ввод не подорожал впервые за три этапа** — не оптимизация,
а место работы: всё новое легло на путь markdown, а стенд ввода печатает
в файл без разметки. Отдельная строка стенда для markdown: 0,8–0,9 мс.

**Первый запуск снова показал себя бесполезным показателем**: 585 и 478 мс
в двух прогонах одной сборки, разброс вдвое больше разницы с прошлой версией.

**Установщик 4,89 → 4,90 МиБ**, плюс 5,2 КиБ. Новых зависимостей нет.

**Приёмка нашла своё**: отрывок в поиске при узкой панели показывает только
то, что стоит **перед** совпадением, — само слово уезжает за правый край.
Поиск исправен, обрезка неудачна. Записано в план этапа 9.

**Задача 59 сделана**: панель закладок — шестая в боковой полосе.

- **Показывает все открытые вкладки** (Р-157), а не только текущую: метки
  у номеров строк и так показывают закладки открытого файла. Ценность
  списка — текст помеченной строки и прыжок в другой файл.
- **Порядок групп — порядок вкладок**, активная наверх не поднимается:
  список, который переставляется при переключении вкладки, читать нельзя.
- **Своего модуля состояния нет**, в отличие от оглавления: там проход
  по всему документу и задержка, здесь — по набору из нескольких закладок.
  Правило: с задержкой считается то, что обходит документ.
- **Закладка на пустой строке подписана «пустая строка»** курсивом:
  показать нечего, а врать пустой строкой нельзя.

**Задача 58 сделана**: читаемая ширина. Настройка `[editor] readable_width`,
по умолчанию включена.

- **Колонка только у markdown** (Р-156): в коде длина строки — часть смысла,
  там выравнивают продолжения и таблицы по столбцам.
- **Колонка включает перенос**, даже когда общая настройка его выключает:
  колонка, которую текст не замечает, — не колонка, а отступ слева. Кто
  переносит markdown руками по 80-му столбцу, тот не заметит разницы.
- **Поля едут вместе с текстом** — это и был вопрос, записанный до кода.
  Номер строки — свойство строки, а не окна; номер, оторванный от строки
  на треть экрана, перестаёт быть её номером.
- **Строка состояния показывает перенос этой вкладки, а не настройку**:
  иначе над переносящимся текстом стояло бы «без переноса». Нажатие
  по-прежнему меняет общую настройку, и подсказка это говорит.
- **Ширина в знаках** (`control-editor-width`, 82ch): мера читаемости
  измеряется знаками, и при другом кегле колонка едет за шрифтом.

**Задача 57 сделана**: оформление markdown в исходнике. Заголовки размером
и весом, жирный, курсив, зачёркнутый, выделение, строчный код, цитата
с чертой слева, приглушённые знаки разметки.

- **Оформление, а не подмена** (Р-152): знаки разметки остаются на экране.
  Горизонтальная черта рисуется дефисами, а не линией; не прячется ничего.
  Правило проверки: **выделить всё, скопировать, вставить в другой
  редактор — получится ровно то, что было на экране.**
- **Размер заголовка — доля, а не пиксели**: размер шрифта редактора
  настраивается, и заголовок едет за ним. Уровней с размером три, дальше
  хватает веса.
- **Цитата — украшение строки, а не диапазона** (Р-153): черту подсветкой
  не выразить, она принадлежит строке целиком.
- **Тест поправил моё ожидание**: строка без `>` сразу за цитатой — тоже
  цитата, так устроен CommonMark (ленивое продолжение абзаца). Украшение
  идёт за разбором, а не за видом строки.
- **`==выделение==` теперь разбирается** (Р-154) расширением на двадцать
  строк — тем же способом, что зачёркивание в GFM. Панель ставила его
  с задачи 43, а разбор не знал: кнопка была, выделения не было.
- **`@lezer/highlight` и `@lezer/markdown` объявлены в package.json.**
  Новых пакетов в дереве не появилось — оба и так стояли зависимостями;
  первый мы импортировали напрямую с этапа 2, не объявив.

**Задача 56 сделана**: плотность, отступы, границы.

- **Пустоту делит черта, а не зазор** (Р-149). Жалоба «пустовато» описывала
  не нехватку украшений, а границы, проведённые пустотой. Между вкладками
  черта; у активной, у её левой соседки и у вкладки под курсором её нет —
  граница там уже проведена заливкой. «Левая соседка активной» достаётся
  только через `:has(+ .tab.active)`.
- **Активная вкладка получает акцентную черту сверху** — внутренней тенью,
  а не рамкой: рамка сдвинула бы содержимое на пиксель.
- **Панель инструментов отделена от текста чертой, а не отступом**, и стала
  одной высоты с прочими полосами инструментов.
- **Размер кнопки — тоже роль** (Р-150, этажом выше Р-096). Панель разметки
  брала размер у навигационной плитки боковой полосы (38 px) и была выше
  всех полос в окне. Заведён `control-toolbar-button-size`, 28 px.
- **Зазор между панелями — либо заметный, либо никакой** (Р-151). Было два
  пикселя при рамке в один: не панель на подложке, а тройной шов. Стало
  четыре. Полоса значков стала такой же панелью, как соседки: раньше она
  была единственной областью без рамки и в светлой теме теряла край.

**Задача 55 сделана**: значки в меню, в палитре и в списке сочетаний.
Нарисовано 55 новых знаков, 14 взяты из готовых.

- **Значок принадлежит команде, а не пункту меню** (Р-148, продолжение
  Р-107). Таблица «команда → значок» одна — `icons/commands.ts`, — и по ней
  рисуют три места сразу.
- **Полнота проверяется тестом**: значок обязан быть у каждой команды
  и у каждого пункта `menu.*`, лишних строк тоже быть не должно. Цепочка
  замкнута: команда в ядре → сверка реестров → значок.
- **Значки девяти пар противоположных действий обязаны различаться** —
  это тоже тест. Одинаковый значок у соседних строк меню сообщал бы,
  что пункты делают одно и то же.
- **Из трёх ответов на «сколько значков» взят «все».** Меню, где значки
  через один, выглядит недоделанным.
- **Узнаваемость рисунка тестом не проверить.** Уголки «свернуть» и
  «развернуть» без древка слиплись при 16 пикселях в звёздочку — увидено
  на снимке живого меню, переделано в стрелки. Родня значка списка задач
  из задачи 43.

**Задача 54 сделана**: шесть тем взамен шести. Тёмные — One Dark, Dracula,
Tokyo Night; светлые — GitHub Light, Solarized Light, Catppuccin Latte;
«Контраст» остался. `midnight`, `nordic`, `pine` и `sepia` исчезли,
`light` и `dark` сохранили идентификаторы.

- **Тест читаемости не смотрел на подсветку** (Р-143), и расширений
  вышло три: все три фона вместо двух (на подложке лежат поля ввода
  и блоки кода в markdown), шесть цветов подсветки — раньше не проверялся
  ни один, и различимость цветов между собой. **Первый прогон
  расширенной проверки на прежних семи темах: 32 нарушения, не прошла
  ни одна.** Хуже всего комментариям — 2,2–3,0:1 везде, кроме «Контраста».
- **Различимость считается расстоянием в CIE Lab**, порог 20. Отношение
  контраста для этого не годится: у двух цветов разного оттенка и одной
  яркости оно равно единице.
- **Адаптация считается, а не подбирается** (Р-146): оттенок и насыщенность
  источника остаются, светлота двигается до порога. Отсюда наблюдение:
  **тёмным темам понадобилось поднять один цвет — комментарий; светлым
  понадобилось всё.** Ни один популярный светлый набор не проходит 4,5:1
  своими цветами, и адаптация светлой темы — это всегда потемнение.
- **Наши «Solarized Light» и «Catppuccin Latte» плотнее оригиналов.**
  Фоны, строй и оттенки их, светлота наша. Сказано и в README,
  и в заголовке файла темы.
- **Строки авторских прав проверены по `LICENSE` в репозиториях**:
  по памяти четыре из шести были неверны.
- **«Как в Obsidian» для markdown — это исходный режим, а не живое
  превью.** Знаки разметки остаются на месте; скрытие знаков меняет
  не вид редактора, а его смысл, и остаётся в «Отложено».
- **Закладки у номеров строк уже есть** с задачи 37 — новая только панель.
- **Снимки в README устарели** — они сняты на «Бумаге». Пересъёмка
  в задаче 60: этап меняет вид ещё четырьмя задачами.
- **Вкладка «Темы» — задача следующего этапа** (Р-147, решение владельца).
  Тема, которой у нас нет, не должна быть задачей разработки: список
  с образцом, «создать свою на основе этой», «открыть папку тем».
  Механика есть вся, не хватает места, где это видно.

**Живое превью markdown входит в область** (Р-155, решение владельца после
задачи 57): `==выделение==` должно показываться выделенным текстом, callout
`> [!tip]` — карточкой. Р-046 и Р-074 в части превью отменены; Р-152 остаётся
описанием **исходного режима** — того, что видно на строке под курсором,
и это ровно то, что сделала задача 57. Уходит в план этапа 9 первым пунктом.
Прятать знаки — это `Decoration.replace`, курсор сквозь спрятанное, виджеты
внутри редактируемой области; переключатель «исходник / живое превью»
обязателен. Инвариант 1 не трогается: файл не меняется ни на байт.

**Этап 9 — «Файлы»: PDF и картинки.** Решения приняты заранее, чтобы
не потерялись. **PDF показывает pdf.js** (решение владельца): встроенный
просмотрщик WebView2 был бы бесплатен по размеру, но потребовал бы ослабить
политику безопасности окна, которая за семь этапов не менялась ни на строку.
Цена выбрана в размере — **около полутора мегабайт, треть установщика**.
**Вкладка, которая не текст, — первое за семь этапов расширение модели
вкладок**, и решение по нему пишется до кода.

**Этап 7 закрыт, метка `v0.7.0`.** «Заметки»: подсказка имён при `[[`,
переименование заметки с обновлением ссылок, оглавление документа, панель
тегов, автосохранение, долги этапа 6. План — `DESIGN.md`, раздел 22;
приёмка — раздел 23; решения — Р-132…Р-142.

**Впервые приложение меняет файлы само**, и приёмка проверяла именно это:
правку ссылок при переименовании (файлы, которые пользователь не открывал)
и автосохранение. Оба пути ведут прямо в инвариант 1, и оба закрыты
побайтными тестами.

Показатели: тёплый старт 406 мс (было 398), ввод под индексацией 3,9 мс
(было 3,7). **Тёплый старт подрос на 8 мс** — плата за пять новых модулей
состояния и две панели; ввод на 0,1–0,2 мс — за разбор контекста `[[`
на каждое нажатие и таймер автосохранения. Тенденция этапа 5 подтвердилась
снова: этап, добавляющий работу на путь ввода, добавляет и десятые доли.

**Установщик 4,85 → 4,89 МиБ**, плюс 41 КиБ: новых зависимостей этап
не принёс ни одной.

**Первый запуск в прогоне — показатель шумный.** 500 и 436 мс в двух
прогонах одной сборки против 415 у 0.6.0: разброс между прогонами того же
порядка, что разница между версиями. Выводы делать только по тёплому старту.

**Р-142 — первый случай за семь этапов, когда решение отменяется не потому,
что обстоятельства изменились, а потому, что оно оказалось неверным**
(Р-038 для перемещения строк).

**Выпуск `v0.7.0` выложен**, обновление проверено через сам механизм:
установленная 0.6.0 нашла 0.7.0, поставила и перезапустилась, сессия
владельца цела. **Версия на машине владельца — 0.7.0.**

**Задача 52 сделана**: долги этапа 6. Перемещение строк отдано CodeMirror.

- **Р-142 отменяет Р-038 для перемещения строк.** Причина не в том, что своя
  реализация плоха, а в том, что она **беднее той, что уже работает в том же
  окне**: `Alt+↑` из `defaultKeymap` двигал все выделенные строки и знал про
  несколько курсоров, а наша `edit.move-line-up` на `Ctrl+Shift+↑` двигала
  строку главного курсора и про остальные молчала.
- **Обещание Р-038 снова выполняется**: все операции со строками работают
  со всеми выделениями сразу. Перемещение было единственным исключением.
- **Своя реализация удалена, а не оставлена рядом**: две реализации одного
  действия расходятся молча.
- **Различий с Notepad++ у взятой реализации не нашлось**: те же строки
  местами, тот же столбец курсора, тот же один шаг отмены.

**Задача 51 сделана**: автосохранение. Настройка `[editor] autosave`,
по умолчанию выключена (Р-133).

- **Поводов два** (решение владельца): пауза 2 с после последней правки —
  та же, что у черновиков, — и потеря фокуса окном или смена вкладки.
  Второй важнее: человек ушёл к другой программе и ждёт свежий файл.
- **Правило отбора одно и оно про диалоги** (Р-141): ручное сохранение
  в трёх местах спрашивает, а диалог, всплывший сам по себе, — худшее,
  что может сделать автосохранение. Пишем только то, что можно записать,
  ни о чём не спрашивая. Не пишутся: буфер без файла, смешанные переносы
  (решение владельца), потери при раскодировании, только для чтения,
  упрощённый режим, расхождение с диском.
- **Вкладка, чей файл исчез с диска, не пишется**, и это нашла живая
  проверка: удалённые файлы вернулись на диск сами через две секунды после
  правки в соседней вкладке. Запись по такому пути **создаёт** файл,
  а создание — действие крупнее обновления.
- **Ошибки записи — в полосу предупреждений**, не в диалог. Список
  заменяется целиком, чтобы одна беда не стала двадцатью сообщениями.
- **Черновики от настройки не зависят**: автосохранение про удобство,
  черновики про инвариант 4.

**Задача 50 сделана**: панель тегов. Пятая панель боковой полосы: теги
проекта с числом заметок, самые частые сверху, поле сужения списка.

- **Панель и палитра отвечают на разные вопросы** (Р-140): палитра ищет тег,
  который знаешь, панель показывает, какие теги есть вообще.
- **Список файлов с тегом панель не показывает**: щелчок уезжает в панель
  поиска — туда же, куда его отправляет палитра с этапа 2.
- **Заодно исправлено давнее**: поле поиска показывает `#тег`, который нашёл
  список. До этого панель открывалась с чужим запросом в поле. Точка входа
  снаружи одна — `openTag`; поиск по набранному зовёт `searchByTag`, там поле
  трогать нельзя.
- **Теги плоские, а не деревом**: щелчок по `работа` и так находит
  `работа/срочное`, так что дерево добавило бы вид, но не смысл.

**Задача 49 сделана**: оглавление документа. Четвёртая панель в боковой
полосе, заголовки списком с отступом по уровню, щелчок ведёт к строке,
раздел под курсором подсвечен.

- **Своя панель, а не часть обратных ссылок** (Р-139): у длинной заметки
  заголовков десятки, и делить высоту значило бы не показывать ни того
  ни другого.
- **Разбор построчный, а не по дереву Lezer.** Дерево ленивое: у большого
  файла оно построено только до видимой части, и оглавление показывало бы,
  докуда долистали. Требовать полного разбора нельзя по инварианту 6.
- **Только заголовки решёткой, и пробел после решёток обязателен.** Без него
  `#работа` в начале строки стала бы заголовком, а не тегом. Подчёркнутые
  заголовки не берём: отличить их от горизонтальной черты можно только
  по контексту блока, а разбор-угадайка показал бы разделы, которых нет.
- **Задержки две.** Список — проход по всему документу, с задержкой 150 мс;
  подсветка текущего раздела — проход по готовому списку, мгновенно.
  Задержать её значило бы показывать не тот раздел, где стоит курсор.
- **За закрытой панелью не считается ничего**: компонента нет.
- **Отступ по уровню берёт `--zn-control-tree-indent`** и задаётся в разметке,
  как в дереве файлов: своя переменная `--zn-*` не прошла бы тест токенов,
  и правильно — префикс принадлежит слою токенов.

**Задача 48 сделана**: переименование с обновлением ссылок. Самая тонкая
работа этапа — впервые приложение меняет файлы, которые пользователь
не открывал.

- **Список изменяемых файлов показывается до правки** (Р-136), диалогом
  с тремя ответами. По умолчанию «Переименовать и обновить» — решение
  владельца: список уже перед глазами, правка обратима и ничего не стирает.
- **План считается симуляцией, а не заменой имени** (Р-137). В откатываемой
  транзакции пути правятся так, будто переименование случилось, каждая
  ссылка разрешается заново, и правится только та, чей ответ изменился.
  Иначе не поймать три случая: тёзку (Р-134), путь вместо имени и — самый
  неочевидный — ссылку **без** пути, которую переименование папки уводит
  в другую заметку, не тронув ни её, ни саму ссылку. Побочная выгода:
  правится минимум, ссылка «которая и так ведёт туда же» не трогается.
- **Форма ссылки остаётся авторской**: путь остаётся путём, имя — именем.
  Иначе переименование папки заодно переписывало бы `[[работа/Планы]]`
  в `[[Планы]]` — правка больше необходимой в чужом файле.
- **Файл читается без приведения переносов строк** и пишется обратно той же
  кодировкой (`document::read_raw`/`raw_to_bytes`). Обычное сохранение буфера
  свело бы смешанные переносы к одному виду — здесь это было бы то самое
  «файл изменился сам». Файл, не декодируемый без потерь, пропускается.
- **Байты по каждому смещению сверяются с ожидаемыми до правки.** План мог
  устареть между показом и согласием; тогда файл не правится вовсе.
- **Вкладка с несохранёнными правками на диске не трогается** (Р-138,
  решение владельца) и показывается в списке отдельной строкой: запись под
  изменённым буфером всё равно была бы затёрта первым же сохранением, зато
  человек получил бы неправдивый вопрос «файл изменила другая программа».
- **Чистые вкладки перечитываются явным вызовом** `checkExternalChanges()`.
  Положиться на сам механизм нельзя: он опрашивает диск при получении окном
  фокуса (Р-014), а во время нашей же правки окно фокус не теряет.
- **Пути вкладок переводятся в новый мир перед сравнением с планом**: план
  приходит с путями после переименования, а вкладка внутри переименованной
  папки лежит ещё по старому — иначе Р-138 обходился бы молча.

**Задача 47 сделана**: подсказка имён при `[[`. Список заметок проекта
всплывает у курсора, стрелки и `Enter` или `Tab` вставляют ссылку, `Escape`
прогоняет. Настройка `[editor] link_suggest`, по умолчанию включена.

- **Вставляется не имя, а та запись ссылки, которая ведёт в выбранную
  заметку** (Р-134). При двух `Планы.md` в разных папках короткое имя
  разрешилось бы в ближайшую — подсказка показала бы одну, а сослалась
  на другую, молча. Текст ссылки подбирает ядро (`link_target`): короткое имя,
  если оно и правда ведёт туда, иначе путь от корня. Правила разрешения
  остаются в одном месте.
- **Список ограничен корнем ссылающегося файла и не содержит его самого.**
  Сослаться на файл из другого проекта нельзя никаким текстом, а ссылка
  на себя не ведёт никуда. Файл вне проектов подсказки не получает вовсе.
- **`autocompletion()` из `@codemirror/autocomplete` не тронут** (Р-135).
  Пакет в зависимостях ради закрытия скобок, границу стережёт
  `tests/brackets.test.ts`, и обойти собственный тест ради удобства значит
  отменить решение, не приняв его. Список свой: `editor/suggest.ts` (разбор),
  `ui/Suggest.svelte` (вид), `placeAtCaret` (положение).
- **Осмотр текста ограничен двумя сотнями знаков перед курсором.** Строкой
  пользоваться нельзя: минифицированный файл в одну строку на мегабайты
  копировался бы на каждое нажатие. Родня Р-101.
- **Escape прогоняет подсказку до конца этой ссылки**, а не просто закрывает:
  контекст сообщается и при смене фокуса окна, и без памяти о прогнанном
  список возвращался бы сам.

- **Подсказка при `[[` — не автодополнение** (Р-132, решение владельца).
  Источник — наш индекс файлов, предлагается имя файла, включается на два
  знака в markdown. Запрет на автодополнение кода и LSP остаётся в силе.
- **Автосохранение — настройкой и выключенное** (Р-133, решение владельца).
  По умолчанию мы редактор файлов, а редактор файлов не пишет в чужой файл
  без команды. Черновики закрывают главный страх и без записи в файл.
- **Задача 48 — самая тонкая:** впервые приложение будет менять файлы,
  которые пользователь не открывал. Список изменяемых файлов показывается
  **до** правки; решение пишется до кода.
- **Обратная связь важнее плана.** Сообщение о нарушении инварианта
  разбирается раньше любой задачи из таблицы.

**Этап 6 закрыт, метка `v0.6.0`.** «Наружу»: версия на виду, разбор
раскладки под VS Code, редактор горячих клавиш, панель разметки markdown,
обновление из GitHub и публикация. **Проект открыт:**
`github.com/Mark-Karte/ZeroNote`. План — `DESIGN.md`, раздел 20; приёмка —
раздел 21; решения — Р-118…Р-131.

Показатели не просели, а впервые за три этапа улучшились: тёплый старт
398 мс, ввод под индексацией 3,7 мс против 4,0 на 0.5.0. Этап не трогал путь
ввода — числа в пределах разброса, и вывод из них «регрессии нет», а не
«стало быстрее».

**Подорожал установщик: 3,92 → 4,85 МБ**, почти мегабайт за HTTP-клиент
с TLS внутри плагина обновлений. Четверть пакета на функцию, работающую раз
в месяц; размен принят сознательно.

**Обновление проверено через сам механизм обновления**, а не тихой установкой
поверх: 0.5.1 → 0.5.2 в задаче 44 и приёмочное 0.5.2 → 0.6.0. Сессия цела.
**От нажатия «Установить» до нового окна около минуты**, и первые тридцать
секунд старый процесс ещё жив, а файл на диске прежний. Проверять раньше —
значит увидеть «не сработало» там, где всё идёт по плану.

**Версия на машине владельца была 0.6.0**, сейчас 0.7.0.

**Долги этапа закрыты этапом 7:** подсказка имён при `[[` — задача 47,
автосохранение — задача 51, перемещение строк — задача 52.

**Задача 45 сделана**: публикация. Репозиторий
`github.com/Mark-Karte/ZeroNote` **открыт**, README переписан для чужого
человека, четыре снимка в `docs/screenshots/`.

- **README — вход, а не документация** (Р-131). Что это, как выглядит, как
  поставить, что умеет, чего не умеет. Устройство и журнал решений остаются
  в `DESIGN.md`. Перечень изменений убран совсем: заметки к версии живут
  на странице выпусков, две копии списка разошлись бы на второй же версии.
- **Снимки сняты на показательном хранилище** в `%TEMP%\zeronote-demo`,
  а не на файлах владельца и не на тестовом наборе с проверками висячих
  ссылок. Перед съёмкой выключены невидимые символы.
- **Имя и почта владельца видны в каждом коммите** — сказано до публикации,
  решение принято осознанно. Историю не переписывали: это рвёт метки,
  к которым привязаны выпуски.
- **Наборы значков macOS и трея убраны** из репозитория, восстановимы
  из истории.

**Задача 44 сделана**: обновление из GitHub. Репозиторий
`github.com/Mark-Karte/ZeroNote` заведён **частным**, выложен настоящий
выпуск `v0.5.1`, кнопка «Обновления» — в карточке «О программе».

- **Закрытый ключ подписи — `C:\Users\user\ZeroNote-signing\zeronote.key`**
  (Р-129), вне репозитория и без пароля: файл и есть секрет. **Копия обязана
  лежать не на машине разработки.** Пока публичного выпуска нет, пару можно
  пересоздать бесплатно; после — нельзя, установленные копии останутся без
  обновлений навсегда.
- **В сеть ходит ядро, а не окно** (Р-130). Политика безопасности вебвью
  не изменилась ни на строку: `connect-src ipc:` как был.
- **`latest.json` пишется руками** — Tauri его не создаёт. Порядок выпуска
  записан в `DESIGN.md`, раздел 20, «Как выпускать версию». Пять шагов,
  забывается третий.
- **Установщик подрос с 3,92 до 4,85 МБ** — цена HTTP-клиента с TLS внутри
  плагина обновлений. Мегабайты двоичные, как во всех приёмках.
- **Значка о новой версии у кнопки сворачивания нет**: он имел бы смысл при
  фоновой проверке, которой у нас нет и не будет.
- **У частного репозитория вложения выпуска доступны только по ключу.**
  Посылка плана «проверим на частном» неверна: анонимно адрес отвечает 404.
  Репозиторий открывался на несколько минут для проверки и возвращён
  в частные — решение владельца.
- **Обновление проверено целиком**: установленная 0.5.1 нашла 0.5.2, скачала,
  проверила подпись, поставила и перезапустилась. Сессия и настройки целы.
  Версия на машине владельца теперь **0.5.2**.
- **Системный диалог в снимок окна не попадает.** `message()` из
  `plugin-dialog` — отдельное окно Windows, и `PrintWindow` приложения его
  не видит. Кнопка при этом остаётся в состоянии «Проверяю…», и это выглядит
  как зависание. Снимать весь экран.

**Задача 43 сделана**: панель разметки markdown. Жирный, курсив, зачёркнутый,
выделение, код, три заголовка, три списка, цитата, ссылка и заготовки —
шестнадцать команд в реестре.

- **Сочетаний у них нет по умолчанию** (Р-127, решение владельца). В Obsidian
  жирный — `Ctrl+B`, но у нас это боковая панель (Р-053), и отнимать её
  у заметок дороже. Назначить можно во вкладке «Клавиши» — задача 42
  оказалась сделана вовремя.
- **Разметка переключается, а не вставляется** (Р-128). Знаки ищутся
  и снаружи выделения, и внутри; у префиксов решение принимается по
  большинству строк; нумерованный список снимается, а не перенумеровывается.
  Всё в `editor/markdown-format.ts` чистыми функциями, 22 теста.
- **Кнопка не забирает фокус**: `mousedown` гасится, иначе команда выполнится
  над редактором, только что потерявшим выделение.
- **Панель только над markdown**, язык берётся у вкладки (его меняют руками
  в строке состояния). Настройка `[editor] markdown_bar`.
- **Значок списка задач перерисован после того, как его увидели на экране**:
  две галочки при 16 пикселях сливались в кашу. Тест реестра значков следит
  за размером и цветом, но не за узнаваемостью рисунка.

**Задача 42 сделана**: редактор горячих клавиш — вкладка «Клавиши» в окне
параметров. Поиск по командам, захват нажатия, столкновения, снятие и сброс.

- **Правка выражается через сочетания, а не через команду** (Р-124). Файл —
  `сочетание = команда`, умолчания в нём не записаны вовсе. Поэтому
  «назначить» — четыре шага, и самый тонкий: снимая умолчание, нельзя
  записывать `""` поверх сочетания, которое пользователь уже отдал другой
  команде. Всё в `keymap/edit.rs`, каждый шаг закрыт тестом.
- **Захват — крючок внутри диспетчера** (Р-125), а не свой слушатель:
  диспетчер стоит на перехвате и вешается при запуске, слушатель позже
  на том же этапе опоздает. Пока идёт назначение, ни одна команда
  не выполняется — проверено, `Ctrl+S` файл не сохраняет. **Захват обязан
  сниматься при размонтировании**: иначе приложение глохнет, и починить это
  с клавиатуры нельзя.
- **Столкновения называются вслух, все три вида** (Р-126): другая команда,
  сочетание самого редактора (Р-122 — его нет в реестре, обычная проверка
  его не видит), буфер обмена вебвью (Р-108).
- **Умолчания приходят из ядра** в `CommandInfo`, а не считаются во
  фронтенде: своя копия таблицы однажды разошлась бы с настоящей.

**Задача 41 сделана**: разбор раскладки под VS Code. Расхождений оказалось
одиннадцать, а не шесть, и половина из них — не расхождения.

- **В окне работает не только наша раскладка** (Р-122). CodeMirror ставит
  `defaultKeymap`, и десяток сочетаний нажимается мимо реестра: `Alt+↑`
  двигает строку, `Shift+Alt+↓` дублирует, `Ctrl+Shift+K` удаляет, `Ctrl+/`
  комментирует. Проверено нажатиями, а не выведено из документации. Наш
  диспетчер стоит на перехвате и глушит событие — наше сочетание всегда
  сильнее, всё остальное достаётся редактору. Список — `DESIGN.md`, раздел 8.
- **Взято в реестр только то, у чего появляется свой смысл**: выделить
  строку, отменить и вернуть курсор, закомментировать. Забирать все примитивы
  правки нельзя: забрав имя, отвечаешь и за поведение, а своя реализация
  бывает беднее чужой.
- **`Ctrl+L` теперь выделяет строку, удаляет `Ctrl+Shift+K`** (Р-120, решение
  владельца). Это было единственное расхождение, которое молча портило текст.
- **`Ctrl+U` вернулся отмене курсора**, `Ctrl+Shift+U` освобождён. У смены
  регистра сочетания больше нет — как в VS Code; команды остались в палитре.
- **Клавиша называется по положению, включая знаки препинания** (Р-121).
  В словаре была одна `comma`, и оттого `Ctrl+/` нельзя было ни назначить,
  ни отнять. Там же нашёлся тихий дефект: строки `ctrl+=` и `ctrl+-` в списке
  отбираемых у вебвью не совпадали ни с чем, и масштаб менялся вопреки коду.
- **Аккордов не будет** (Р-123, решение владельца) — строка в «Отложено».
  Решать надо было до задачи 42: редактор клавиш под одно нажатие
  переделывается под два не бесплатно.

**Задача 40 сделана**: версия на виду. Карточка «О программе» в окне
параметров, команда «Версия и сведения о программе» в палитре, копирование
сведений одной кнопкой.

- **Версию среды спрашиваем у системы, а не у строки агента** (Р-119).
  Edge сокращает `navigator.userAgent`: там `152.0.0.0`, а на машине стоит
  `152.0.4191.62`. Точным числом это не является, но выглядит им — и разбор
  отчёта уходит не туда. Берётся `tauri::webview_version()`, команда
  `commands/about.rs`. Разбор строки агента убран совсем: запасной путь,
  тихо дающий неверный ответ, хуже отсутствия ответа.
- **Карточка версии стоит снаружи `{#if values}`**: на вопрос «какая
  у вас версия» надо отвечать и при испорченном `settings.toml`. Проверено
  на живом приложении со сломанным файлом.
- **Подпись команды начинается со слова «Версия»**: в палитре ищут по тому
  слову, которое в голове, а спрашивают всегда версию.
- **Ссылки на страницу выпусков нет** до задачи 44 — решение владельца.
  Репозитория не существует, адрес пришлось бы угадывать.
- **Первое нажатие после смены переднего плана стенд теряет.** `keys.ps1`
  и `click.ps1` в новом процессе иногда съедают первый ввод: палитра остаётся
  открытой, кнопка не нажимается. Второй такой же вызов срабатывает. Не
  чинить приложение по одному снимку — повторить нажатие.

**Сеть появляется только по нажатию и только с подписью** (Р-118, решение
владельца). До этого этапа приложение не открывало ни одного соединения.
Фоновых проверок нет, наружу не уходит ничего, кроме номера версии, у
обновлений своя пара ключей — не тот сертификат, которого нет по Р-007.
**Потеря закрытого ключа означает, что установленные копии не обновятся
никогда.**

**Репозиторий заводится частным** в задаче 44 и становится открытым
в задаче 45: обновляться неоткуда, пока нет настоящего выпуска, а проверка,
которая не ходит в сеть, не проверяет то, ради чего написана.

**Этап 5 закрыт, метка `v0.5.0`.** «Каждый день»: контекстное меню, свёртка
блоков, парные скобки, отступы по содержимому, невидимые символы, закладки,
операции с файлами в дереве. План — `DESIGN.md`, раздел 18; приёмка — раздел 19;
решения — Р-106…Р-117.

Показатели не просели: тёплый старт 403 мс, ввод под индексацией 4,0 мс.
Ввод подорожал на 0,3 мс — этап добавил пять постоянно включённых расширений
редактора, и это записано как тенденция, за которой следят.

**Задача 38 сделана**: операции с файлами в дереве. Создать файл и папку,
переименовать, удалить — только в корзину (Р-110).

- **Мимо корзины не удаляем никогда, и мер две.** До удаления спрашивается
  `SHQueryRecycleBin` про том: нет корзины — отказ, ничего не тронуто.
  Плюс флаг `FOF_WANTNUKEWARNING` — он предупреждает ровно там, где система
  собралась удалить насовсем (файл больше корзины). Это **третий `unsafe`**
  в проекте, `src-tauri/src/fsx/recycle.rs`.
- **Открытые вкладки переезжают вместе с файлом**, включая случай
  «переименовали папку». Команда ядра `move_buffer` меняет путь и имя
  и не трогает признак изменения: файл тот же, правки на месте.
- **Вкладки удалённого закрываются без вопроса**: спрашивать «сохранить
  перед закрытием» значило бы предлагать создать файл заново.
- **`F2` в дереве работает, когда фокус в дереве** (Р-117). Щелчок по файлу
  уводит фокус в редактор — там `F2` снова про закладки. В VS Code так же.
- **У корня нет переименования и удаления**: за ним тянутся сессия,
  наблюдатель и индекс. Для него есть «Убрать папку».

**Задача 37 сделана**: закладки. `Ctrl+F2` поставить или снять, `F2` дальше,
`Shift+F2` назад, по кругу. Помечается ячейка с номером строки.

- **Закладки живут в `EditorState`** (Р-116), а не списком сбоку: `RangeSet`
  переносится через правку сам, и закладка уезжает вместе со своей строкой.
  В сессию уходят номера строк — файл могли поправить снаружи.
- **Переключение закладки не меняет ни текст, ни выделение**, и слушатель
  обновлений его не видел: в сессию уезжали вчерашние закладки, а на экране
  метки стояли. Родня Р-105. У изменения закладок теперь свой обработчик.
- **`F2` в задаче 38 придётся делать контекстным**: в тексте закладка,
  в дереве переименование — как `Tab` по Р-115. В VS Code он тоже контекстный.

**Задача 36 сделана**: невидимые символы. Пробелы точкой, табы стрелкой,
переносы знаком абзаца. Настройка `[editor] invisibles`, команда
`view.invisibles` в палитре и в контекстном меню.

- **Перенос помечается одним знаком**, а не «CRLF» и «LF»: документ внутри
  всегда с `
`, настоящий тип переноса живёт в модели буфера. В файле
  со смешанными переносами разные подписи были бы враньём.
- **Цвета CodeMirror перекрыты токенами**: точка у него `#aaa`, стрелка —
  картинкой с зашитым `#888`. Стрелка нарисована знаком, а не картинкой:
  цвет внутри `data:`-URI переменной не задать.
- **Переключателя в строке состояния нет, вопреки плану.** В VS Code строка
  состояния показывает свойства файла, а переключатели вида — в меню
  и палитре. У нас там уже есть «перенос» от прежнего ориентира; двигать
  его дороже, чем не добавлять второй.

**Задача 35 сделана**: отступы. Определяются по содержимому файла, настройка
`[editor] indent_style` и `indent_width` — только умолчание (Р-106).
Определённое видно в строке состояния, там же меняется для одной вкладки.

- **Смена отступа не переписывает файл.** Меняется только то, чем набирается
  новый; преобразование уже набранного — отдельная явная команда, которой
  пока нет (инвариант 1).
- **`Tab` — единственное сочетание вне реестра** (Р-115). Вне текста `Tab`
  означает переход по элементам, и отнять его глобально нельзя. До задачи 35
  он вообще не был занят и уводил фокус из редактора.
- **Отступ — свойство вкладки**, как язык: у файла с табами `Tab` даёт таб,
  даже когда в настройках пробелы.

**Задача 34 сделана**: парные скобки. Подсветка пары, переход к паре
(`Ctrl+Alt+B`) и автозакрытие настройкой `[editor] auto_close`, по умолчанию
включённой.

- **В прозе кавычка сама не закрывается** (Р-113): markdown и текст без
  языка закрывают только `(`, `[`, `{`. «Сказал» — не строковый литерал.
  Задано данными языка, а не своим обработчиком ввода.
- **Что подсвечено, к тому и переходим.** Порядок поиска пары взят у подсветки
  CodeMirror, включая «курсор внутри только что набранной пары».
- **`@codemirror/autocomplete` в зависимостях ради `closeBrackets`** (Р-112).
  Автодополнение остаётся вне области; границу стережёт тест
  `tests/brackets.test.ts`, а не память.

**Задача 33 сделана**: свёртка блоков. Всё пришло из разбора языка — своих
правил вида «скобка открывает блок» нет ни одного, заголовки markdown
сворачиваются до следующего того же уровня. Поле свёртки справа от номеров
строк, значок — уголок из дерева.

- **Alt с цифрой на дополнительной клавиатуре сочетанием не считается**
  (Р-111). `Alt+0151` — это тире, а не начало `Alt+0`. Цифра верхнего ряда
  сочетанием остаётся: Windows эти клавиши различает, и мы теперь тоже.
- **Свёртки нет у языков из `legacy-modes`** (TOML, YAML, INI, SQL, Shell,
  PowerShell, Lua) и у обычного текста: разбор там построчный, дерева нет.
  Свёртка по отступам — в «Отложено».
- **`SendKeys` шлёт нажатия без скан-кода**, и от этого пуст `event.code`,
  на котором держится вся раскладка. Две проверки подряд выглядели дефектами
  приложения — виноват был стенд. Сочетания слать только через
  `keys.ps1 -Chord "ctrl+shift+p"`.
- **Меню закрывается от любой клавиши**, а не только от Escape.
- **Удаление файлов в дереве будет, но только в корзину** (Р-110, решение
  владельца, задача 38). `SHFileOperationW` с флагом «в корзину» удаляет
  мимо неё молча, когда корзины нет (сетевой диск) или файл не помещается:
  правильный ответ там — отказ с объяснением, а не удаление насовсем.
- **Окон ZeroNote при отладке два** — установленное и `target\debug`.
  Скрипты стенда теперь требуют `ZERONOTE_PID`, если находят больше одного.
  И после перемещения окна первый щелчок приходит в вебвью с координатами
  от прежнего места: полтора часа задачи 32 ушли на «ошибку в расчёте
  положения меню», которой не было.

**Задача 32 сделана**: меню вебвью снято во всём окне, своё меню есть
в редакторе, на вкладке, в дереве (файл, папка, корень, пустое место)
и в поле ввода. Пункты — из реестра команд, сочетания настоящие. Добавились
«копировать путь», «копировать имя», «показать в проводнике», «закрыть
другие».

Что из этого стоит помнить:

- **Буфер обмена на своих клавишах остаётся за вебвью** (Р-108). `Ctrl+X`,
  `Ctrl+C`, `Ctrl+V` до наших обработчиков не доходят: CodeMirror умеет
  построчное копирование и раздачу строк по курсорам, и подражание вышло бы
  хуже. Команды в реестре есть — ради меню и палитры; сверяется пара
  «команда — сочетание», поэтому переназначение работает, а подпись в меню
  не врёт.
- **Чтение буфера обмена — в ядре** (Р-109). `navigator.clipboard.readText()`
  в нашем WebView2 не отвечает вовсе: обещание не разрешается и не
  отвергается. Ветка ошибки никогда не срабатывает, «Вставить» молча ничего
  не делает. Запись при этом браузерная и работает. Это **второй `unsafe`
  в проекте** — `src-tauri/src/clipboard.rs`.
- **Правый щелчок по вкладке сначала делает её активной.** Иначе
  «Сохранить» из меню третьей вкладки сохранило бы первую.
- **Контекстный пункт действует на то, по чему щёлкнули**, и команды
  в реестре у него нет — у клавиши нет объекта под курсором. Такие пункты
  зовутся `menu.*`, а не `file.*`.
- **Меню рисуется в `AppShell`**, а не там, где вызвано: внутри боковой
  панели оно уезжало бы с прокруткой и обрезалось бы по краю.

Два правила этапа, записанные заранее:

- **Отступ определяется по файлу, настройка — только умолчание** (Р-106).
  Настройка «два пробела», применённая к чужому файлу с табами, смешала бы
  одно с другим первым же нажатием `Tab`. То же правило, что у кодировки
  и переноса строк: файл диктует, настройка подсказывает.
- **Пункт меню не заводит своё действие** (Р-107). Он ссылается на команду
  из реестра `keymap/` и оттуда же берёт подпись сочетания. Иначе меню
  и клавиши разъедутся, и заметить это можно будет только глазами.

Публикация в открытый доступ снова отложена: пока в редакторе не хватает
вещей, которые чужой человек заметит на первом часу, показывать его рано.

**Этап 4 закрыт, метка `v0.4.0`.** Отделка: свой знак, честный масштаб
значков, позиция курсора, блоки кода в markdown, заметка по висячей ссылке,
переделанный замер инварианта 6. План — `DESIGN.md`, раздел 16; приёмка —
раздел 17; решения — Р-096…Р-105.

Задачи 25–31 сделаны: набор владельца из `icons/` собирается
в `src-tauri/icons/` скриптом `icons/make-icons.ps1`, знак рисуется разметкой
и следует теме, размер значка стал ролью, в строке состояния появилась позиция
курсора, блоки кода в markdown получили подложку, подпись и копирование,
`Ctrl`+щелчок по висячей ссылке создаёт заметку, стенд инварианта 6 печатает
в настоящую вкладку.

Показатели не просели: тёплый старт 373 мс, ввод под индексацией 3,7 мс —
и это впервые измерено тем же путём, каким печатает пользователь.

**Стенд инварианта 6 теперь идёт тем же путём, что пользователь** (задача 30).
Первой строкой оставлен прежний путь мимо приложения — как база: разница
и есть цена обвязки, около 0,4 мс. Переделка сразу нашла дефект, который
ручные проверки не находили с этапа 1 (Р-105).

**Живое представление правится, только если в нём лежит именно эта вкладка**
(Р-105). Условия «вкладка активна» мало: у буфера без языка подстановка языка
не содержит `await` и выполняется синхронно, когда в представлении ещё прошлая
вкладка. Присваивание её состояния новой вкладке теряло содержимое, а путь
оставался свой — то есть сохранение записало бы в файл чужой текст.

**Где создаётся заметка по ссылке — решают правила разрешения ссылки**
(Р-098, Р-104), а не удобство: косая черта в цели означает путь от корня,
её отсутствие — рядом с ссылающейся заметкой. Иначе файл создался бы не там,
где его потом ищет та же ссылка. Цель с `..`, запретным знаком в имени или
внутри `.obsidian` отвергается с внятным сообщением.

**Оформление блоков кода едет вместе с разбором markdown**, а не в общем наборе
расширений (`langs.ts`): в файле `.rs` весь текст и так код. Подпись показывает
не написанное в ограждении, а то, чем мы язык признали, — и говорит «нет
подсветки», когда не признали (Р-103). Обходятся только видимые строки: блок
бывает длиной в файл.

**Стенд инварианта 6 печатает в свой `EditorView`, а не во вкладку** (Р-102).
Значит, строка состояния в измерение не попадает — и вывод приёмки этапа 3
о цене счётчика курсоров был получен на пути, где счётчика не было. Сейчас
работа строки состояния меряется отдельной колонкой; переделка стенда на
настоящую вкладку — в «Отложено». Всё, что кладётся в строку состояния, лежит
на пути ввода: проверять цену, а не предполагать.

**Столбец считается в единицах UTF-16** (Р-101). Это постоянное время; честный
подсчёт символов означал бы просмотр начала строки на каждое нажатие, а такие
файлы — минифицированные, в одну строку на мегабайты — открывают именно
в редакторе этого класса.

**Размер значка значок себе не назначает** (Р-096, Р-100). Он наследует
`--zn-control-icon-size`, как наследует цвет через `currentColor`; умолчание —
строка списка, а контекст с другой ролью переопределяет переменную у себя:

```css
.button { --zn-control-icon-size: var(--zn-control-icon-size-window); }
```

Ролей пять: окно 10, плитка 20 (боковая полоса и карточка параметров),
знак 20 и 32, строка 16. У значка нет и не должно быть свойства «размер»:
точек вызова тридцать четыре, контейнеров с ролью — пять.

**Знак приложения — единственная двухцветная иконка** (Р-099). Ему разрешён
акцент и только токеном; всем прочим — по-прежнему `currentColor`. Список
исключений держит тест, а не договорённость.

**Этап 3 закрыт, метка `v0.3.0`.** Оформление по дизайн-референсу «Slate»:
семь тем (заменены задачей 54), вшитые
шрифты, шапка с крошками, панели с зазорами, палитра на три режима, окно
параметров, стартовый экран, мультикурсор, перенос строк. План и приёмка —
`DESIGN.md`, разделы 14 и 15; решения — Р-074…Р-095.

Показатели не просели: тёплый старт 398 мс, ввод под индексацией 3,4 мс.
Обновление поверх 0.2.0 проверено на живой установке: `settings.toml`
не тронут, сессия восстановлена, файл настроек без нового раздела `[editor]`
прочитан на умолчаниях.

Главное правило этапа: **референс адаптируется, а не копируется** (Р-074).
Где он расходится с моделью, побеждает модель: вкладки остаются документами,
графа связей не будет, окно без внешнего поля.

Что легко нарушить в оформлении:

- **Читаемость темы считается, а не оценивается на глаз** (Р-078, Р-079).
  Тест `builtin_themes_are_readable` меряет контраст по WCAG на обоих фонах —
  рабочей области и панели. При первом прогоне он поймал три цвета, которые
  я до того посчитал верными. Новая тема — прогнать тест.
- **Цветов кода в палитре шесть** (Р-082). Операторы, знаки препинания
  и заголовки markdown берут цвет из общего текста. Захотелось седьмой —
  сначала проверить, точно ли роль про язык.
- **Начертаний три**: `normal` 400, `medium` 500, `strong` 600. До задачи 17
  `medium` означал 600, и старые вызовы переведены на `strong`.
- **`fonts.css` выведен из-под проверки на литералы**, но тест следит, что
  в нём нет ничего, кроме `@font-face`. Не класть туда оформление.
- **Три слоя фона, и путать их нельзя** (Р-083, он же уточняет Р-080).
  `bg-canvas` — подложка окна, видна только в зазорах; `bg-surface` — панели;
  `bg-raised` — рабочая область, диалоги, всплывающие. Поле ввода внутри панели
  берёт `bg-canvas`: оно утоплено, а не приподнято. В светлой теме подложка
  темнее панелей, в тёмной — светлее; порядок ролей от этого не меняется.
- **Вид панели задаёт класс `panel`**, стиль для него один — в `AppShell`
  через `:global`. Не повторять скругление и рамку в каждом компоненте.
- **Вид файла — `icons/files.ts`**, одна таблица расширений на значок и на цвет
  (`color-file-*`). Второй такой таблицы заводить не надо: я уже завёл и убрал.
- **Углы окна прямые, и это решено** (Р-086). `DwmSetWindowAttribute` возвращает
  успех и не делает ничего — у окна без обрамления нет рамки для скругления.
  Не переоткрывать без готовности делать окно прозрачным.
- **Строка пользователя не идёт и в `LIKE`.** `_` и `%` там служебные, и запрос
  `план_б` без экранирования находит `планаб`. Ошибка тише, чем со скобкой
  в FTS5: запрос не падает, а молча возвращает лишнее.
- **Режим палитры задаёт точка входа, а не остаток прошлого раза.** `Ctrl+P`
  всегда файлы, `Ctrl+Shift+P` всегда команды; набранное сохраняется.
- **Приложение теперь пишет в `settings.toml`** (Р-089 отменяет ограничение
  Р-013). Только через `settings/edit.rs` на `toml_edit`, только по одной
  настройке, и итог обязан пройти наш `settings::parse` до записи. Испорченный
  файл не переписывается вовсе.
- **Окно параметров показывает наши ключи**, а не список из референса: там
  автосохранение и превью markdown, которых у нас нет. Настройка, которой
  не существует, — это обещание, которое некому выполнить.
- **`bench/ui/keys.ps1 -NoClick`** — если открыта палитра, модалка или список
  выбора. Обычный вызов сначала щёлкает в рабочую область и тем самым их
  закрывает. Координаты щелчка — только именованным `-At "x,y"`: без этого
  первое нажатие уезжало в координаты, а скрипт всё равно отчитывался
  «отправлено».
- **История недавнего — свой файл `data/recent.toml`** (Р-090), не поле сессии.
  Пишется только на явное открытие; восстановление сессии в неё не попадает.
- **Сочетания в интерфейсе берутся из раскладки**, а не пишутся в разметку:
  стартовый экран и палитра показывают то, что и правда нажимается.
- **`Ctrl+D` — мультикурсор, дублирование строки на `Ctrl+Shift+D`** (Р-091).
  Единственное расхождение с раскладкой Notepad++, сделано по решению владельца.
- **Перенос строк — настройка `[editor] wrap`**, общая, не по вкладке (Р-092).
  Применяется отсеком CodeMirror: пересоздание состояний стёрло бы историю
  отмены во всех файлах разом.
- **В диалоге по умолчанию всегда безопасный вариант** (Р-093). Необратимый
  помечается `danger: true` и виден цветом. Enter не должен стирать данные.
- **Полоса предупреждений скрывается по набору сообщений**, а не насовсем
  (Р-094): новая жалоба возвращает её.
- **Единственная анимация — появление диалога и меню** (Р-095). Смещение
  из токена отступов, длительность и кривая — из токенов движения.

Правила по проектам, дереву и индексу, которые легко нарушить не подумав:

- **В папку пользователя не пишется ничего** без явной команды (Р-049).
  `zeronote.toml` создаётся только по кнопке и с переспросом.
- **Настройки проекта не назначают кодировку, а только подсказывают**
  (Р-050…Р-052 рядом). `default_encoding` подменяет догадку определителя
  и никогда — метку порядка байтов или годный UTF-8.
- **Дерево не обходится целиком — ни в ядре, ни во фронтенде** (Р-054).
  Раскрыли папку — прочитали её, и только её. Полный обход есть только
  у индекса: это другой потребитель с другими требованиями.
- **Индекс — кэш, а не данные пользователя** (Р-058…Р-062). Чужая версия
  схемы, испорченный файл, потеря последних записей — всё это лечится
  перестроением, а не миграцией и не сообщением об ошибке.
- **Правила игнорирования действуют и на индекс.** Файл, скрытый в дереве,
  но находимый поиском, — утечка, а не удобство.
- **Языки подсветки грузятся по требованию** (Р-066). Добавляя язык, класть
  его в реестр `editor/langs.ts` через `import()` внутри загрузчика, а не
  обычным импортом: иначе парсер уедет в стартовый пакет.
- **Цвета подсветки — токены, роли общие для всех языков** (Р-047, Р-067).
  Стиль ссылается на CSS-переменные, поэтому смена темы работает без
  пересборки. Проверять обе темы: работает только в одной — дефект токенов.
- **Переходник Obsidian только читает `.obsidian`** и переносит одну
  настройку — фильтры исключения (Р-071). Ключ, который ничего не делает,
  в наш формат не заводим, даже «на будущее».
- **Связи разбираются только у markdown, и не внутри кода** (Р-069).
  `#include` в блоке C++ — не тег, `[[i]]` в примере — не ссылка.
- **Ссылка разрешается при запросе, а не при записи в индекс.** Заметка могла
  появиться позже ссылки на неё; при разрешении на записи такая ссылка
  осталась бы висячей навсегда.
- **`lower()` в SQLite умеет только латиницу.** Путь с кириллицей ею
  не найдётся — приведение к нижнему регистру считаем в Rust и храним
  отдельной колонкой.
- **Строка пользователя не идёт в запрос FTS5 как есть.** Скобка в поисковой
  строке — синтаксическая ошибка запроса, а не поиск скобки. Оборачиваем
  каждое слово в кавычки.

## Стек

Tauri v2 · Rust · TypeScript + Svelte 5 · CodeMirror 6 · **Lezer** (подсветка,
этап 2) · SQLite FTS5 через `rusqlite` (этап 2) · `notify` (задача 10) ·
`ignore` (задача 9) · **pdf.js** (`pdfjs-dist`, задача 71).

`pdf.js` — единственная крупная зависимость фронтенда и выбор владельца
(Р-181): встроенный просмотрщик WebView2 был бы бесплатен по размеру,
но потребовал бы ослабить политику безопасности окна. Своими руками PDF
не рисуют — это отдельный продукт. Обошёлся почти во весь прирост
установщика за этап — 471 КиБ.

Вся тяжёлая работа — файловый ввод-вывод, кодировки, обход дерева, индексация —
на стороне Rust. Фронтенд занимается отображением и вводом.

**Исключение — подсветка синтаксиса.** Она на Lezer, во фронтенде, и это
осознанно: документ уже живёт там (Р-002), а гонять диапазоны подсветки через
IPC на каждый символ противоречит инварианту 6. Решение Р-042 — там же почему
не Tree-sitter.

TypeScript зафиксирован на 6.x: `svelte-check` не поддерживает 7.x.

## Как писать код

**Про Rust.** Владелец проекта знает Rust слабо и учит его на этом проекте.

- Каждый нетривиальный кусок Rust сопровождать коротким пояснением: почему такой
  тип владения, зачем здесь `Arc`/`Mutex`/канал, что делает эта аннотация времени
  жизни. Если пояснение нужно и через полгода — комментарием в коде, иначе в чате.
- Предпочитать простые и очевидные конструкции сложным и «идиоматичным», если
  разница в производительности не измерима. Читаемость важнее.
- Не разбрасывать `unsafe`, макросы и хитрые обобщённые типы. Любой `unsafe`
  требует отдельного обоснования и записи в журнал решений.
- Если запрос основан на неверном понимании языка — сказать прямо и объяснить,
  как правильно.

**Зависимости.** Перед добавлением любой библиотеки — сначала обосновать, почему
без неё нельзя, и записать обоснование в `DESIGN.md`. Минимум зависимостей,
максимум простого собственного кода там, где библиотека решает лишь малую часть
задачи.

**Никаких заглушек.** Если что-то не реализовано — явная пометка `TODO` и строка
в плане, а не фиктивное поведение, изображающее работу.

## Оформление

Механика оформления — часть архитектуры, а не косметика.

- **Ни одного зашитого цвета, отступа, радиуса, тени или размера шрифта в коде
  компонента.** Только CSS-переменные из слоя токенов. Это проверяется тестом
  `tests/tokens.test.ts` — он ловил нарушения пять раз, в том числе мои.
  Цвета подсветки синтаксиса — тоже токены (Р-047). Он же сверяет, что каждая
  переменная `--zn-*` объявлена: `var(--zn-опечатка)` молча пуста, свойство
  отбрасывается, и элемент едет на умолчании браузера.
- Тема — файл в папке пользователя, TOML, применяется без перезапуска.
- Светлая и тёмная обе, плюс следование системной. Работает только в одной —
  это дефект токенов.
- Иконки — через реестр по логическим именам (`file.markdown`, `action.save`),
  не вставляются в разметку напрямую.
- Всё настраиваемое живёт в читаемом конфиге, который можно править руками.
  Интерфейс настроек — надстройка над файлом.
- **Гибкость не должна протекать в код компонентов.** Если новая настройка
  требует ветвлений по всему интерфейсу — архитектура выбрана неверно, сказать
  об этом сразу.
- Настраиваемое и красивое — разные задачи. Первая с самого начала, вторая —
  этап полировки. До тех пор базовая тема сдержанная и нейтральная.

## Производительность

Цели — часть спецификации. Регрессия считается дефектом. Замеряется на каждом
этапе, результаты пишутся в `DESIGN.md`, раздел «Измерения».

| Показатель | Цель |
|---|---|
| Холодный старт до готовности к вводу | ≤ 2000 мс |
| Тёплый старт | ≤ 800 мс |
| Открытие файла 5 МБ | ≤ 500 мс |
| Ввод символа при открытом файле 10 МБ | без заметной задержки |
| Свыше 50 МБ | упрощённый режим: чтение, без подсветки и индексации |
| Раскрытие папки с 10 000 записей | ≤ 300 мс |

```
npm run tauri build          # замеры только на релизной сборке
powershell -File bench\perf.ps1
```

```
powershell -File bench\crash-test.ps1
```

Последний проверяет инвариант 4 на живом процессе: правит файлы через интерфейс,
убивает процесс и смотрит, что восстановилось.

Если задача упирает в потолок — сообщить и предложить варианты, а не занижать
планку молча.

## Порядок работы

- Одна задача — один коммит с осмысленным сообщением.
- **После каждого этапа — сжатие контекста.** Перед ним всё, что понадобится
  дальше, должно лежать в `DESIGN.md` и здесь, а не в переписке.
- После каждого шага: что сделано, что осталось, что требует решения владельца.
- Каждое существенное решение — в `DESIGN.md`, журнал решений, в том же коммите.
- Тесты обязательны для всего, что касается файлового ввода-вывода, кодировок и
  сессии. Для интерфейса — по обстоятельствам.

## Проверки перед коммитом

Те же три идут в GitHub Actions на каждый push в `main`
(`.github/workflows/ci.yml`). Локальный прогон это не отменяет: узнать
о поломке через шесть минут после пуша дороже, чем за минуту до него.

```
npm run check
```

```
npm test
```

```
cd src-tauri && cargo test
```

## Особенности окружения

- **Файлы `.ps1` сохранять с BOM (UTF-8 with BOM).** Windows PowerShell 5.1
  читает скрипты без BOM как ANSI, и кириллица в них ломает разбор.
- **`Get-Content` для файлов, записанных из Rust, — только с `-Encoding UTF8`.**
  Иначе PS 5.1 прочитает UTF-8 без BOM как ANSI.
- Сборка релиза идёт минутами (`lto = true`, `codegen-units = 1`) — запускать
  фоном, а не блокировать сессию.
- **`src-tauri/target/debug` растёт до десятков гигабайт** (2026-09-06 он был
  44 ГиБ при 3 ГиБ всего остального). Чистится безопасно: всё внутри
  пересобирается, а `target/debug/data` — данные отладочного запуска, их
  стоит оставить. Цена очистки — одна полная пересборка отладочной сборки.
- **Тёплый старт меряется, когда другого окна ZeroNote нет ни одного.**
  Живой сосед держит среду WebView2 тёплой и занижает число на четверть:
  та же сборка дала 422 мс без соседа и 292 мс с ним (приёмка этапа 9).
  С задачи 69 сосед **с той же папкой данных** замер не искажает, а срывает:
  второй экземпляр отдаёт свои аргументы первому и выходит, отчёта не будет
  вовсе. Отладочная сборка и установленная держат разные папки и друг другу
  по-прежнему не мешают.
  Там же второе: замер восстанавливает сессию из папки данных выпускной
  сборки, и её содержимое на число влияет — сессию для замера надо задавать.
- **`std::env::temp_dir()` бывает коротким именем.** На машине непрерывной
  сборки это `C:\Users\RUNNER~1\AppData\Local\Temp`, и путь оттуда — другая
  запись той же папки, чем та, что хранит приложение после `root::normalize`.
  Тесты, сравнивающие пути, обязаны нормализовать (Р-163).
- **Подписанную сборку запускать из bash, а не из PowerShell.** У ключа нет
  пароля, и Tauri требует, чтобы `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` была
  задана **пустой**; а `$env:VAR = ""` в PowerShell переменную не задаёт,
  а **удаляет**. Сборка при этом идёт до конца, печатает
  «expect a prompt for password» и встаёт насмерть: ввода нет. Порядок выпуска
  в `DESIGN.md`, раздел 20, написан на bash не случайно.
- **`0xFFFFFFFF` в PowerShell 5.1 — это Int32 −1, а не 4294967295.** Отсюда
  `-120 -band 0xFFFFFFFF` даёт −120, а `[uint32](-120)` падает. Падение при
  этом **не видно**: ошибка непрерывающая, цикл идёт дальше, и скрипт бодро
  отчитывается об успехе. Так первая `scroll.ps1` «прокручивала», не прокручивая
  ничего. В скриптах стенда — `$ErrorActionPreference = 'Stop'`.
- **Байтовые литералы `b"..."` в Rust принимают только ASCII.** Для кириллицы —
  обычная строка и `.as_bytes()`.
- **`r#"..."#` ломается о `"#` внутри** — а это любой шестнадцатеричный цвет
  в тесте. Использовать `r##"..."##`.

## Ручные проверки в живом приложении

Дважды за этап 1 проверка молча проверяла не то. Правила, выведенные оттуда:

- `SetForegroundWindow` из фонового процесса Windows **блокирует**. Нажатия
  уходят в чужое окно — однажды прямо в проводник. Забирать передний план
  через `AttachThreadInput` и **проверять результат**, а при неудаче падать.
- `SendKeys` считает служебными `+ ^ % ~ ( ) { } [ ]` — их надо оборачивать
  в фигурные скобки, иначе регулярное выражение доедет искалеченным.
- Событие получения фокуса не сработает, если окно и так было активным:
  фокус нужно по-настоящему увести и вернуть.
- Результат правки сверять **по байтам файла**, а не по снимку экрана.
- Для снимка окна надёжнее `PrintWindow` с флагом `PW_RENDERFULLCONTENT`
  (`0x2`): передний план из фонового процесса дают не всегда, а `PrintWindow`
  работает и с неактивным окном. Без флага содержимое WebView2 в снимок
  не попадает — получится пустой прямоугольник.
- **Скрипты стенда лежат в `bench/ui/`** — снимок, нажатия, щелчок, наведение,
  прокрутка. Не писать их заново: там уже учтены все грабли ниже.
- **Прокрутка — `scroll.ps1`**, и она понадобилась на выкладке 0.10.0: у окна
  параметров содержимое длиннее экрана, кнопка «Обновления» под сгибом,
  а клавишами её не достать — прокручивается вложенная область без фокуса.
  Запасной путь без скрипта — щелчок по дорожке полосы прокрутки
  (`click.ps1 1194 700`), он листает страницами.
- **Системное окно живёт в своих координатах.** Снимок `shot.ps1` даёт
  координаты от угла окна ZeroNote, а диалог обновления — отдельное окно
  Windows, и попасть в его кнопку можно только по снимку всего экрана
  и щелчку по абсолютным координатам. Числа из этих двух систем не смешивать:
  на выкладке 0.10.0 они разошлись на 220 пикселей по вертикали, и щелчок
  «по кнопке» уехал в список настроек.
- **Координаты дерева съезжают, когда появляется полоса вкладок.** Открыв
  первый файл, все строки уезжают вниз на её высоту — и следующий щелчок
  по «тем же» координатам попадает в соседнюю строку. Я потратил на это
  полтора часа, разыскивая несуществующую регрессию: снимок надо делать
  непосредственно перед щелчком, а не в начале проверки.
- **`SendKeys` не вводит кириллицу при английской раскладке системы.**
  Буквы просто не появляются, а скрипт отчитывается «отправлено» — тот же
  вид, что у прошедшей проверки. Латиница идёт всегда. Проверки ввода
  строить на латинских именах либо готовить текст в файле заранее
  и дописывать одну букву (задача 82).
- **`keys.ps1 -At x,y`** щёлкает прицельно, а не в середину окна: в середине
  лежит текст, и щелчок туда двигает курсор.
- **Перед щелчком окно прибивается к началу экрана.** Между вызовами оно
  переезжает, и координаты от прошлого положения промахиваются на сотни
  пикселей — при этом скрипт отчитывается об успехе.
- **Переднего плана мало для клавиатуры.** WebView2 держит содержимое
  в дочернем окне, и пока щелчка внутрь не было, фокус остаётся на рамке:
  нажатия приходят в приложение и не доходят до вебвью. Сначала щелчок
  в рабочую область, потом ввод. Проверять на команде, которая точно
  работала раньше (`Ctrl+N`), — иначе будешь чинить приложение вместо стенда.
- Для щелчка передний план всё-таки нужен. Помогает нажать и отпустить `Alt`
  перед `SetForegroundWindow`: Windows отдаёт передний план охотнее, если
  только что был ввод с клавиатуры.
- **`GetWindowRect` шире видимого окна**: в него входит невидимая рамка
  изменения размера. Координаты из снимка и координаты для щелчка расходятся
  на пару десятков пикселей — то есть на строку-другую списка. Целиться надо
  по проверке, а не по вере: щёлкнуть, снять экран, убедиться, что попал.

Общее правило: проверка, которая не может провалиться, выглядит точно так же,
как проверка, которая проходит.

## Структура

```
src/                фронтенд: ipc/ editor/ ui/ theme/ icons/ keymap/ state/ actions/
src-tauri/src/      ядро: commands/ text/ fsx/ model/ project/ tree/ session/
                    settings/ keymap/ theme/
src-tauri/tests/    интеграционные тесты; fixtures/ — 17 эталонных файлов
bench/              perf.ps1 — замеры, crash-test.ps1 — инвариант 4,
                    ui/ — стенд ручных проверок в живом приложении
design/             дизайн-референс этапа 3 и его разбор
icons/              знак приложения: исходники, разбор, сборка .ico
```

`commands/` не содержит логики — только маршалинг в `text/`, `fsx/`, `model/`.
Поэтому вся логика тестируется обычным `cargo test` без запуска приложения.

Что добавится на этапе 2 — `DESIGN.md`, раздел 12.

## Приёмы, устоявшиеся за этап 1

Повторять, а не изобретать заново:

- **Канонический список — в Rust, потребитель — во фронтенде, тест сверяет.**
  Так сделаны токены оформления (`tokens.rs` ↔ `tokens.css`) и команды раскладки
  (`keymap/mod.rs` ↔ `keymap/registry.ts`). Расхождение валит сборку.
- **Конфиги: читаем терпимо, ошибаемся громко.** Отсутствующий ключ берёт
  умолчание; опечатка называется по имени и показывается полосой предупреждений;
  приложение остаётся работоспособным на умолчаниях.
- **Образец файла с комментариями кладём при первом запуске** и никогда не
  перезаписываем существующий.
- **Ошибки пользователю, а не в лог.** Полоса предупреждений вверху окна.
- **Свой модальный диалог** (`state/modal.svelte.ts`): системный умеет два
  ответа, а «отмена» вместо «не сохранять» — это тихая потеря данных.
- **Тяжёлое — с задержкой.** Черновики через 2 с, счётчик совпадений через
  150 мс. Мгновенное остаётся мгновенным.
